# KumiPlayer 2.0 问题根因与施工方案（2026-09-15）

> 状态：待用户审阅  
> 用途：集中记录本轮用户反馈的全部问题的根因、证据、解决办法与施工批次，供外部审阅与后续编码交接  
> 事实来源：当前源码（`backend/app/`、`src/`）、`backend/tests`、以及**用户真实库 `data/kumiplayer.db` 的只读 SQLite 取证**  
> 优先级：用户指令 > 当前源码与测试 > 本文 > 其他历史文档

## 0. 阅读须知

### 0.1 状态用语

| 用语 | 含义 |
|---|---|
| 已定位 | 有源码行号或只读数据证据支撑的根因结论 |
| 待确认 | 需要运行态（devtools / 实际播放 / 真实网络请求）才能定论 |
| 待实现 | 尚无生产实现 |
| 批次 | 建议的施工顺序，每批可独立提交与验证 |

### 0.2 本轮取证方式

- 只读取证：6 路并行代码取证（元数据状态、来源卡统计、识别门槛、前端 UI、OpenList 扫描与增量、目录树解析差异、MPV 链路），全部只读，未修改任何文件。
- 只读数据取证：以 `mode=ro` 打开用户真实库，读取 `works / scrape_bindings / artifacts / source_scans / source_roots / revision_issues` 等表；未执行任何写入，未触发任何真实库修复。
- 本地二进制取证：`third_party/mpv/runtime/mpv.exe --list-options`（v0.41.0）。
- 未执行：未打包、未运行全量测试、未对 TMDB/OpenList 发起新的网络请求。

### 0.3 一句话总览

| # | 用户看到的现象 | 根因一句话 | 误报 / 真实缺陷 |
|---|---|---|---|
| P-01 | 刮削结果正确却报「在线资料服务暂不可用」，「重新获取」无变化 | 两条独立路径：① provider 调用异常 → `source_unavailable`（但会保留上一轮成功资料，所以详情仍完整）；② 图片产物缺失被完整性门控改写成 `failed`，且文案统一成「服务不可用」。重试会真正重跑，但输入不变 → 结果重复 | 状态语义与文案误报 + 图片产物真实缺失 |
| P-02 | 来源卡「5 个待处理事项」「11 项关联信息待补全」 | 5 = 上述 5 部电影的失败计数（真实）；11 = 动画电影被生成指向**自身标题**的伪父系列，永远无法满足 | 11 项 100% 误报 |
| P-03 | 标题很明确却必须人工复核 | 一条取不到「作品容器」的文件（如根目录下的 `SP01.mkv`）无条件标记 `needs_review`，一条脏数据阻断整批确认；且 `4k` 前缀不会被清洗，导致在线候选永远无法自动采用 | 门控误报 + 清洗缺口 |
| P-04 | 修正/重命名弹窗标签与输入框贴合，X 像独占一行；OpenList 三个按钮竖排 | 组件间零间距声明 + Portal 丢失页面作用域 + 一条**选择器优先级冲突**（`.media-v4-command-row > div` 击败 `.media-v4-command-buttons`） | 真实 UI 缺陷（纯样式为主） |
| P-05 | OpenList 全量扫描「卡住不动」 | 读取阶段总量恒为 0（无百分比）+ 单次递归全量列目录，实测 244 秒后被上游 500 判失败；失败即整轮作废 | 真实缺陷（进度语义 + 扫描策略） |
| P-06 | 115 与百度目录树规则不同，需分别适配 | 现有解析器用一套**跨文件粘性**规则处理两种格式：115 当前输出正确（实测 `动画/…`，2472 条），但状态脆弱——`.rmvb` 缺失、无正文 provider 探测、混入 `\|-` 行的百度树会被多裁一层 | 真实缺陷（范围已按 §0.4 收窄） |
| P-07 | 想用别人的 MPV 整合包 + 只加载自己的插件 | 现状用 `--config-dir` 指向 KumiPlayer 自有配置，属**替换**语义，整合包配置会被整体绕过 | 架构选择（可行性已验证） |

---

## 0.4 第二轮复核勘误（2026-09-15，必读）

外部审阅对本文件提出 4 条证伪。我已逐条独立复核（源码 + 直接调用生产函数），结果如下：

| # | 原文结论 | 复核结果 | 更正 |
|---|---|---|---|
| 1 | 115 真实样本首行 `\ufeff\|——根目录` 从未被 `_PAN115_ROOT_LINE` 匹配，当前正确路径是"靠巧合" | **原文错误** | 直接调用生产函数 `read_directory_tree_text()` 读 `docs/samples/根目录20260703203700_目录树.txt`：返回文本**不含 U+FEFF**（`_decode_tree_bytes` 对 `FF FE` 选 `"utf-16"`，Python 解码即剥离 BOM），首行 `\|——根目录` 被 `_PAN115_ROOT_LINE.match()` 命中（**True**），解析出 2472 条，相对路径以 `动画/…` 开头。原探针在整段文本上用 `findall` 且未带 `re.M`，属**探针方法错误**，不能作为论据 |
| 2 | OpenList 增量"仍会核对所有目录，`verification_budget` 只影响顺序" | **原文错误** | `incremental.py:258-262` 队列为 `["", *rolling]`，`rolling` 只取最久未验证的 `[:budget]` 个已知目录；只有**新发现或 mtime 明确变化**的子目录才继续入队（`:330-345`）。增量确实受预算限制。真正的缺口是：TXT 基线目录 `modified=None` 导致"首次远端核验"没有状态迁移、`changed` 判定要求前后都非 None、默认预算仅 12（上限 50） |
| 3 | `/api/v4/metadata/retry` 是"确定性空操作" | **表述错误** | `scrape.py:135-177 requeue_work` 只重置 job 状态，但 `process()` 随后**真正重新执行** provider 与图片下载（`scrape.py:285`）。准确说法：重试会真正重跑，它不改变任何输入（配置 / binding / 缓存），因此在故障条件不变时结果必然重复——这正是"点了没变化"的原因；网络或代理恢复后可以成功 |
| 4 | 图 1 的"在线资料服务不可用"与"缺海报"是同一件事 | **过度归因** | `source_unavailable` 产生于 provider 调用异常（`metadata.py:818-837`），artwork 完整性只在 provider 已 ready 之后才运行（`scrape.py:366-391`），是**两个不同阶段**。正确解释见 §1.1 修订说明 |

复核中另外确认了两条原文漏掉的事实：

- **`reason_code` 空值缺口（已按第二轮审阅校正）**：`scrape.py:384-391` 在完整性失败时只写 `reason` 与 `completeness`，**没有写 `reason_code`**。但"补一个字段就够"是**过头**的说法：`service.py:282` 虽然认识 `artifact_incomplete`，却只把它映射回现有的 `retry_metadata`；`_friendly_metadata_reason()` 没有该原因码的专用文案，仍会因 `status == "failed"` 显示「媒体信息处理未能完成，请重试」；前端也只按 action 渲染，按钮仍叫「重新获取媒体信息」。因此这条止血必须**同时改三处**（见 §8 的 B1-1）。
- **重试按钮的来源**：`service.py:268-294` 的 fallback 对 `status == "failed"`（`:290-291`）与 `"source_unavailable"`（`:286-287`）都返回 `retry_metadata`，因此这两类作品都会显示「重新获取媒体信息」并真正重跑刮削——与用户观察到的"点了没变化"一致。

---

## 1. P-01 元数据状态误报：「在线资料服务暂不可用」+「重新获取无变化」

### 1.1 实测数据（用户真实库，只读）

当前活动 revision = `rev-234cb06a…`（confirmed 2026-09-15T08:03:16Z，41 work / 41 mirror / 41 scrape）。实测分布：

| 分类 | 数量 | 状态 |
|---|---|---|
| 番剧（anime_series） | 30 | ready |
| 动画电影（anime_movie） | 6 | ready |
| 动画电影 | **5** | 非 ready |

界面显示的「41 部作品 / 36·5 待处理 / 番剧 30 / 动画电影 6」与之**逐一吻合**。5 部非 ready 全部是动画电影：

| 作品 | TMDB | binding.status | metadata_state | reason | retryable |
|---|---|---|---|---|---|
| 福音战士新剧场版：Q | 75629 | source_unavailable | source_unavailable | 在线资料服务暂不可用，请稍后重试（TMDBClientError） | **true** |
| 天空之城 | 10515 | failed | failed | 海报下载或发布失败；背景图下载或发布失败 | false |
| CLANNAD 剧场版 | 16516 | failed | failed | 海报下载或发布失败；背景图下载或发布失败 | false |
| 魔女宅急便 | 16859 | failed | failed | 海报下载或发布失败 | false |
| 龙猫 | 8392 | failed | failed | 海报下载或发布失败 | false |

5 部的 `metadata_json` 都**包含完整的标题、原名、年份、评分、时长、首播日期、简介、类型**（即用户看到的「刮削结果都是对的」），且 `poster_url` 都是合法的 `https://image.tmdb.org/t/p/w780/…`。

逐目录核对 `data/mirror`：天空之城、CLANNAD 剧场版只有 `clearlogo.png` + `movie.nfo`；魔女宅急便、龙猫、Q 有 `fanart.jpg` + `clearlogo.png`；**5 部都缺 `poster.jpg`**。其余 36 部 ready 作品的 `poster.jpg` 均存在。

**结论：缺的只是海报文件，但状态被写成"在线资料服务不可用"，而「重新获取」虽然会真正重跑刮削，但在故障条件不变时结果必然重复（见 §0.4 勘误 3）。**

**结论（按 §0.4 修订）**：5 部作品都缺 `poster.jpg`，但其非 ready 原因并不相同：Q 当前失败于 provider 刷新，另外 4 部失败于本地产物完整性门控。

**修订说明（原表述过度归因）**：5 部作品其实是**两条不同路径**——

- 福音战士新剧场版：Q 是**真实的 provider 异常**（`metadata.py:818-837` 在 detail 请求抛 `TMDBClientError` 时写 `source_unavailable`）；它之所以仍显示完整资料，是因为 `scrape.py:360-365` 会调用 `_retain_successful_details`，把上一轮成功取得的标题、原名、年份、评分、简介等字段原样保留（`scrape.py:24-61`）。→ **"本轮在线刷新失败" 与 "页面资料完整" 可以同时成立**，UI 应表达为「已保留上次成功获取的资料，本次刷新失败」。
- 另外 4 部（天空之城、CLANNAD 剧场版、魔女宅急便、龙猫）是**资料已经 ready、只有图片产物缺失**，被完整性门控改写成 `metadata_state="failed"`。

### 1.2 根因链

**根因 A：图片产物缺失被写进"资料状态"，且文案统一成服务不可用。**

- `backend/app/media_v4/jobs/scrape.py:366-374`：只有 `metadata_state == "ready"` 才发布产物；
- `scrape.py:376-391`：`assess_metadata_completeness` 不通过 → `metadata_state = "failed"`、`reason = "；".join(reasons)`、`completeness = reasons`，**此时 `reason_code` 仍为空**；
- `backend/app/media_v4/jobs/completeness.py:52-78`：`artwork_storage_mode = "local"` 时，必须存在**已发布且非空**的 `poster` 与 `fanart` 文件，否则返回「海报下载或发布失败 / 背景图下载或发布失败」；
- `backend/app/media_v4/revisions/service.py:83-88`：把 `state == source_unavailable` 映射成「在线资料服务暂不可用，请稍后重试」，把 `state == failed` 映射成「媒体信息处理未能完成，请重试」。

于是"少一张图"在界面上等价于"刮削服务挂了"，与用户看到的完整刮削结果直接矛盾。

**根因 B：图片下载失败被静默吞掉，无法诊断。**

- `backend/app/media_v4/jobs/metadata_artifacts.py:131-174`：poster 是下载循环里的第一个产物，异常在 `151-154` / `168-171` 被吞，既不写 `artifacts` 行，也不落盘、不记日志，只在 completeness 里留下一句中文；
- `metadata_artifacts.py:95-100`：下载客户端用 `proxy=config.proxy_url or None`，http2，超时 3-12 秒；
- 用户 `data/config.json` 的 `proxy_url = http://127.0.0.1:7890`，而**该端口当前未监听**（同一死代理也是本轮 `git push` 与 P-05 的失败背景）；
- 5 部作品的失败集中在第 1、2 个图片请求，第 3 个（clearlogo）全部成功 → 符合"新建客户端后首批请求失败"，而非逐图 404（**具体 HTTP 原因待确认**，本轮未联网复现）。

**根因 C：重试会真正重跑，但不改变任何输入，因此结果必然重复（原文"确定性空操作"表述不准，见 §0.4 勘误 3）。**

- `backend/app/api/media_v4.py:2086-2111` 读取最新 binding 计算 policy；`backend/app/media_v4/jobs/scrape.py:135-177` 的 `requeue_work` **只** `UPDATE jobs SET status='queued'`，不清 binding、不清 `metadata_json`、不改配置；
- provider 是同一 `target` 的纯函数（`scrape.py:285`；`metadata.py:603-612` 走 trusted binding → 同一个 TMDB 请求）；
- 实测：Q 的 scrape job `attempts=2`（08:07:52→08:08:04，其余 40 个 `attempts=1`），binding 于 08:08:04 被重写，结果仍是 `source_unavailable` → **按钮确实执行了，只是结果必然相同**。

**根因 D：图片缺失还导致作品从媒体墙消失。**

- `backend/app/api/library_v4.py:170-172` 只保留 `metadata_state == "ready"` 的作品 → 5 部作品不出现在列表（才有 30+6=36）。

### 1.3 解决办法与施工方案

**P-01a 拆分状态（批次 B1，后端）**

- 保留 `metadata_state` 只表达 provider 身份与资料获取结果；新增**产物状态**（`poster_state` / `fanart_state` / `clearlogo_state` / `nfo_state`，或统一 `artwork_state` + `artifact_errors[]`），持久化进 `scrape_bindings.metadata_json` 与 job 结果；
- `completeness` 失败不再把 `metadata_state` 改写成 `failed`，而是写入 `artwork_missing` 之类的 `reason_code`；
- 前端（`src/components/media/V4ExecutionProgress.tsx:59-76,191-206`）分别展示「资料已获取」与「图片缺失（可重试下载）」，不再出现"服务不可用"。

**P-01b 图片下载可诊断、可重试（批次 B1）**

- 每个产物独立重试（3 次 + 抖动退避），失败写入 `artifacts` 行 `status='failed'` 并记录 `error`（HTTP 状态码 / 超时 / 代理错误）；
- 失败必须进日志与错误日志中心（复用 `core/error_log.py`），不再静默；
- 新增"只重下缺失产物"的恢复动作（`retry_artifacts`），不重跑 provider；重试前清掉该产物的失败缓存，保证输入真的变化；
- 配置面增加「图片源连通性测试」（TMDB API + `image.tmdb.org`），失败时给出可操作提示，例如「本机代理 `127.0.0.1:7890` 不可连接，请启动代理或清空代理设置」（项目已有同类做法：`backend/app/integrations/bangumi.py:313-315`）。

**P-01c 媒体墙可见性（批次 B1，需产品决策，见 §9 决策点 1）**

- 建议：图片缺失**不应**让作品从媒体墙消失；以占位图显示并在卡片上标一个轻微提示。当前 `library_v4.py:170-172` 的 ready 过滤把"导入成功但缺图"变成"作品不见了"。

**P-01d 文案与恢复动作（批次 B1）**

- `service.py:56-89` 的文案映射按新状态重写；`metadata_recovery_action` 增加「重新下载图片」，并保证 `retryable=false` 时前端不渲染必然无效的重试按钮（现状 `service.py:246-250` 对 `episode_not_found(retryable=false)` 仍给 `retry_metadata`）。

**验证（B1）**

- 新增/更新针对性回归：`backend/tests/` 中 completeness、metadata state、artwork 下载、recovery policy 相关用例；用隔离 SQLite fixture，语义覆盖：图片失败 → 资料 ready + 图片缺失；图片重试成功 → 状态收敛；代理不可用 → 提示可操作错误；
- 前端：`src/components/media/V4ExecutionProgress.tsx` 相关组件测试；
- 不修改真实库，用户自行重试验证。

---

## 2. P-02 来源卡两个数字

### 2.1 「5 个待处理事项」= 真实

- `backend/app/media_v4/projection/source_libraries.py:347-361` `_execution_attention_count`：按作品去重统计 `work_units` 中 `overall_status ∈ {failed, needs_attention}` 的作品数 + 无作品归属的失败 job 数；
- 作品状态由 `backend/app/media_v4/revisions/service.py:430` 推导（`scrape_status not in {ready, confirmed} → needs_attention`）；
- 与只读实测的 5 部非 ready 电影完全一致 → 这个数字是**对的**，问题在 §1 的状态语义。

### 2.2 「11 项关联信息待补全」= 100% 误报

- 计算公式：`source_libraries.py:87-93` 统计 `revision_issues` 中 `code='unresolved_parent_relation' AND resolved=0`，再经 `:364-376` `_relation_is_pending` 只读重估后输出到 `:335`；
- 产生点：`backend/app/media_v4/revisions/service.py:1836-1848`，message 形如「父系列 `series:<key>` 尚未导入，无法建立作品关系」；
- 伪父键来源：**`backend/app/media_v4/resolution/resolver.py:184-190` 对 `child_media_type == "movie"` 且 `relation_type ∈ {movie, spin_off, recap, related}` 强制把父 `media_type` 改成 `"tv"`**，使 `:197-199` 的自名抑制判断（`media_type == child_media_type`）永远不成立 → 生成 `series:<自身标题>:tv`；
- 只读实测：11 条全部形如 `series:龙猫:tv`、`series:天空之城:tv`、`series:福音战士新剧场版:q:tv`，`child` 就是该 active 电影自身（如 `title:龙猫:1988:movie`）→ 父系列永远查不到，永远 pending；
- `git blame`：`184-190` 来自 `074fa46d`，`197-199` 来自 `94bcd53a`（属近期回归）。

### 2.3 解决办法（批次 B2，后端）

1. **修正父关系生成**：电影（`media_type == "movie"`）不应生成父系列关系；自名抑制必须在强制 `media_type` 之前按**规范化标题等值**判断，而不是按 `media_type` 字符串相等；
2. **历史行只读收口**：`_relation_is_pending` 已经是只读重估点，扩展为「child 为 movie 且父键等于 child 自身标题 → 非 pending」，即可让来源卡数字归零，**不需要修改真实库**（符合项目"真实错误数据原样保留"的硬规范）；
3. 回归：`backend/tests/` 中 `unresolved_parent_relation` 与身份关系相关用例；验证 `source_libraries` 卡片计数。

---

## 3. P-03 识别自动通过率：明显标题也要人工复核

### 3.1 实测复现（与用户截图同症状）

输入：`4k偶像大师 灰姑娘女孩 U149/01.mkv … /12.mkv` + **根目录直属** `SP01.mkv`，`root_container` = 该目录名。

结果：12 条 season facts 全正常；`SP01.mkv` 得到 `work_title=""`、`confidence="low"`、`needs_review=True`；`series_group` / `title_candidates` 其实已经是**正确的完整标题**；Resolver 输出恰好 1 条 `parsed_facts_need_review`，message 与界面逐字一致（「解析结果标记为需要人工复核，确认前必须处理」）。只读实测该导入的 revision `rev-daa874…`：29 条 evidence、1 条 issue、状态 draft。

### 3.2 根因

**根因 A：`needs_review` 是文件级布尔标记，与"作品身份是否可得"完全解耦。**

- `backend/app/recognition/media.py:265-273`：非 local/baidu 且路径只有 1 段（例如所选根目录直属的文件）时，作品容器返回 `""`；
- `media.py:1971-1973` 回退到文件名推断，`SP01.mkv` 无法给出作品名；
- `backend/app/recognition/title_cleaner.py:185-186`：容器为空 → `needs_review=True`（无条件）；
- `media.py:2207-2208` / `2264-2267` → `backend/app/media_v4/parsing/parser.py:614` → `backend/app/media_v4/resolution/resolver.py:388-395`：**只看 `facts.needs_review` 就生成阻断 issue，不看该条目的作品身份是否已经解析成功**；
- `src/pages/MediaManagementPage.tsx:1439,1450`：前端只要 `issues.length > 0` 就禁用「确认」；唯一出口是「修正」（`:768` 固定回传 `needs_review:false`）。

即：**12 集正常 + 1 个特别篇文件把整批导入卡死**，而用户认为"名字已经很明显"。

**根因 B：`4k` 前缀不被清洗 → 在线候选永远无法自动采用。**

- `title_cleaner.py:32-38` 的画质清洗只处理**尾部**（要求前置分隔符且位于末尾），`4k` 只作为方括号 token 关键词（`:53`、`:94-101`、`:150-167`）参与过滤；
- 实测 `4k偶像大师 灰姑娘女孩 U149` → `changed=False`、`applied_rules=[]`；
- `backend/app/media_v4/resolution/ranker.py` 的规范化后等值判定：`4k偶像大师灰姑娘女孩u149` ≠ TMDB 的 `偶像大师灰姑娘女孩u149` → `_title_identity_level = 0`、score 15.0 < 阈值 55.0、`identity_safe=False` → **即使 TMDB 有正确条目也永不自动采用**；
- 阈值位置：`ranker.py:22-23`（MIN_SCORE 55.0 / MIN_MARGIN 10.0）、判定 `:350-383`；`high` 置信定义 `backend/app/media_v4/resolution/candidates.py:82-84`（规范化**完整等值** + 年份不冲突）。

### 3.3 解决办法（批次 B3）

1. **门控纠偏**：`resolver.py:388-395` 改为「`needs_review` **且** 该条目作品身份不可得」才阻断；`parser.py` 在用选择根目录名成功补出 `series_group`/`title_candidates` 时清除该条目的 `needs_review`；
2. **容器回退**：`media.py:265-273` 在路径只有 1 段时，回退到"本次选择的根目录名"作为作品容器（这是 OpenList/网盘单作品目录的常见形态），而不是返回空；
3. **前置画质前缀清洗**：在标题规范化阶段剥离前置/独立出现的画质 token（`4k/8k/2160p/1080p/uhd/hdr` 等），使本地标题与在线标题可等值；注意保留"标题本身含 4k"的极端情形（可用「剥离后仍能与其他候选等值」作为判定条件，避免误伤）；
4. **issue 分级**：后端给每条 revision issue 增加 `blocking` 字段；前端只对 `blocking=true` 的 issue 禁用确认，非阻断项降级为提示（现状全部靠"issues 非空"一刀切）；
5. **验证**：`backend/tests/test_v4_*.py` 中解析/解析器/候选相关用例 + 新增「根目录直属 SP 文件」「前置 4k 前缀」「单作品目录 OpenList 导入」三个夹具；前端 `tests/components/recognition-identity-dialog.test.tsx` 等同步。

---

## 4. P-04 前端 UI 缺陷（截图可见）

### 4.1 「修正识别结果」弹窗（`src/components/media/V4RecognitionSummary.tsx`）

证据：
- `:114-121` Dialog → DialogSurface → FluentProvider → DialogBody → `DialogTitle action={<Button … Dismiss24Regular/>}`
- `:136-165` 在 `.media-v4-override-row.media-v4-override-dialog-row` 里平铺 4 个 `Field`：`:137-144` 作品标题（**`:141` 的 placeholder 也是「作品标题」**，与 label 重复）、`:145-154` 媒体类型、`:157-159` 季度、`:160-162` 集号
- CSS：`:13735` 页面用 5 列网格；`:13902` 弹窗内覆盖成 1 列；`:13900` 只把 `.fui-DialogBody` 的 gap 改成 16px；**全库对 `.fui-Field` 只有 `:14031-14042`，且属于另一个弹窗**；`.media-v4-issue-evidence` **完全没有样式规则**

根因：
1. Field 的 label/控件/hint 间距零声明，全靠 Fluent 默认（label 2px + 2px，hint `margin-top:2px`），叠加根字号 17px / 行高 1.6（`src/styles/foundation.css:24-25`）→ 观感为「作品标题作品标题」贴合；
2. label 与 placeholder 文案重复，放大了重复感（结构性）；
3. 同一个 `.media-v4-override-row` 既是页面 5 列行内网格、又是弹窗 1 列竖排，只靠一条覆盖救回，属脆弱耦合；
4. `X` 独占一行：本弹窗没有任何 `.fui-DialogTitle*` 规则，标题行布局完全交给 Fluent（DialogBody 为 `1fr 1fr auto`，action 在 `row1/col3`），应用层只把 gap 从 8px 加到 16px → X 被拉开。**是否真换行需运行态 devtools 核对 computed `grid-template-columns`（待确认）**。

### 4.2 「重命名来源卡」弹窗（`src/pages/MediaManagementPage.tsx:1556-1575`）

- `:1564` `<Field label="来源名称" hint="仅修改来源卡显示名，不会改动实际目录。">` + `:1565` `<Input>`
- CSS `:13937-13950` surface 用**半透明** `--surface-raised`（fluent `rgba(255,255,255,.9)`、mica `.92`、cinema `rgba(42,46,54,.9)`），对比识别弹窗用的不透明 `--surface-solid`（`:13895`）→ 背后来源卡文字透出，加重"文字压边"观感；
- `:14031-14042` 只给 `.fui-Field/.fui-Input` 定义宽度与文字颜色，间距仍用 Fluent 默认 → hint 压在输入框下边界。

**判定：纯样式**（Field 三行间距、hint margin、不透明底色），JSX 用法正确。

### 4.3 OpenList 导入第 1 步命令区与进度行（`MediaManagementPage.tsx:1359-1427`）

- **三个按钮竖排的真正原因是一条选择器优先级冲突**：`:13643 .media-v4-command-row > div { display:grid; gap:2px }`（特异性 0,1,1）**击败** `:13721 .media-v4-command-buttons { display:flex; flex-wrap:wrap; gap:10px; justify-content:flex-end }`（0,1,0）→ 按钮容器实际是 2px 单列网格；按钮又被 `:5303` 的 `white-space:nowrap` 与 `:5314` 的 `min-width:148px` 拉成同宽，整块被 `:13642` 的 `space-between` 推到右侧 → 观感"三个无关按钮"；
- `:13787-13788` 的窄容器规则永不生效（死代码）；
- 进度行：`:13552` 定义一个 `width:min(620px,100%)` 的 2 列内网格，却被放进 MessageBar 自身的 4 列网格（且 `:1414` 未传 icon），首行"Spinner + 标题"左对齐、百分比右对齐到 620px、进度条被 `grid-column:1/-1` 强制换行 → 三条基线不成一组；窄容器再整体翻转对齐（`:13780-13781`）。

**判定：纯样式**，JSX 不必改（建议给按钮组加显式 class，不再依赖 `> div` 通配）。

### 4.4 三主题影响

- 三套主题只改颜色/材质，不改上述布局；三个弹窗各自嵌套 `FluentProvider`，Portal 内 Fluent 组件的间距与主题无关；
- 唯一跨主题相关命中：`:6330-6333` cinema 下 `[class*="fui-Label"]{color:var(--text)!important}`（会覆盖 `:14035` 的 label 颜色）与 `:13512`；
- **Portal 是本轮 UI 缺陷的结构性背景**：`.media-flow-page .fui-Button`（`:5303`）、`.media-primary-command.fui-Button`（`:5314`）、`@container media-flow`（`:13761-13795`）在 Portal 弹窗内**全部失效**，所以 `:13900-14003`、`:14031-14042` 才有那批"重声明"。

### 4.5 施工方案（批次 B4）

1. 在 `src/styles/` 新增**弹窗/表单基础层**（归属 Cascade Layer），统一 `.fui-Field` 的 label/控件/hint 三段间距、对话框标题行网格、`DialogActions` 按钮区（标题、正文、操作区三层明确）；
2. 修掉 `.media-v4-command-row > div` 与 `.media-v4-command-buttons` 的优先级冲突：给按钮组显式 class 并从通配规则中排除；命令区改为"文案左、动作右、窄容器换行且按钮横排"；
3. 进度行重排：把 stage、百分比、进度条放进同一个受控网格，避免嵌入 MessageBar 的多列网格；
4. 修正识别弹窗：把「作品标题」的 label 与 placeholder 文案去重；四个 Field 成组并给字段级说明；若运行态确认 X 换行则重建标题行；
5. 底色统一：来源卡弹窗改用不透明 `--surface-solid`；
6. **验证**：`tests/` 中结构敏感用例（见下）必须同步；改动后至少执行 `npm run test:contracts`、`npm run test:components`、`npm run build`，并做浅色/深色/窄窗口的实际渲染核对（**当前 `node_modules` 缺失，需先 `npm install`**）。

受影响的现有测试（改动前必读）：`tests/media-management-ui-contract.test.ts:30-93`（CSS 正则与类名顺序）、`tests/components/media-management-overview.test.tsx:224-257`、`tests/components/recognition-identity-dialog.test.tsx:32-39`、`tests/components/media-management-page.test.tsx:143-250`。

---

## 5. P-05 OpenList 全量扫描卡死 + 增量与风控

### 5.1 实测数据（用户真实库）

| scan | root | 开始 | 结束 | 耗时 | 结果 |
|---|---|---|---|---|---|
| scan_936 | 夸克 `/夸克网盘/动画` | 08:50:16 | 08:54:20 | **244s** | failed：`OpenList 请求失败（500）` |
| scan_de6 | 同上（gen2） | 09:09:07 | 09:13:11 | **244s** | failed：`OpenList 请求失败（500）` |
| scan_5c8 | 百度 `K:\百度网盘\01动画`（TXT 来源） | 08:03:08 | 08:03:12 | 4s | completed 907 条 |
| scan_751 | 夸克单作品目录 | 09:13:57 | 09:13:59 | 2s | completed 29 条 |

**结论：小目录没问题；大目录整树递归在 244 秒后被 OpenList 上游 500 判失败。界面上表现为"进度卡住不动"，因为读取阶段根本没有百分比。**

### 5.2 根因

1. **读取阶段总量不可知 → 无百分比**：`backend/app/media_v4/sources/scanner.py:590-600`（OpenList full）、`:460-477`（本地）、`backend/app/media_v4/sources/incremental.py:378-382`（增量）都传 `total_count=0`；真正的总量要到扫描返回后才由 `_settle_total`（`source_scan_runner.py:478-489`）写入；`durable_scan.py:402-404` 在 `total==0` 时 `progress=None` → 前端进度条只能是不确定态。
2. **长同步段零进度、零心跳**：`scan_finalize.py:91` 的整批归一化与 `:102-118` 的 `create_draft` 大事务期间不写心跳；心跳只在 `_update_scan_progress`/节流分支里写（`durable_scan.py:131-135`、`source_scan_runner.py:279-284`）。静默超过 120 秒还会被判定为 `interrupted`（`durable_scan.py:397-401`），前端直接提示"上次扫描意外中断"。
3. **单次递归全量列目录 + 限速放大**：每请求间隔 ≥ 1 秒（`integrations/openlist/governor.py:26,70-81`），每页 100 条（`scanner.py:549`），目录数 N 的库读取阶段至少 N 秒且无反馈；全链路没有墙钟超时，唯一总量护栏是 20,000 条（`scanner.py:513-514,552-553`），触发即**整轮失败**。
4. **远端读取阶段没有目录级 checkpoint（原文"失败即全废"表述过头）**：`source_scan_runner.py:458-459` 异常一律落 `failed`，没有部分成功。注意 durable scan **已有** stale recovery：证据完整且已过读取阶段时可从 SQLite evidence 续跑 finalizer，也可用 input archive 重排同一 scan（`source_scan_runner.py:500-610`）；**真正缺的是"最后成功目录 / 下一页游标"**，所以远端读取中断只能从头再来。
5. **增量确实受预算限制（原文"仍会核对所有目录"错误，见 §0.4 勘误 2）**：`incremental.py:258-262` 队列为 `["", *rolling]`，`rolling` 只取最久未验证的 `[:budget]` 个已知目录；只有新发现或 mtime 明确变化的子目录才继续入队（`:330-345`）。真正的缺口是三条：① TXT 基线目录 `modified=None`（`:57-71`），首次远端核验没有"unknown → verified"的状态迁移；② `changed` 判定要求前后都非 None（`:337-341`），因此 `None → 具体 mtime` 不算变化；③ 默认预算仅 12、上限 50（`:29-30,240`），大库首次远端核验需要多轮轮转。
6. **变化判定只比对路径与目录 mtime**（`:331-341,347-371`）：size 与内容指纹都不参与判定；改名等同删+增。建议补 `direct_listing_hash`（对直接子项的 name/is_dir/size/modified 求哈希），避免把网盘 mtime 的稳定性当成唯一依据。
7. **风控可用手段与缺口**：限速 1 req/s（无 burst）、单 worker 串行、`max_entries/max_depth`、冷却（risk_control 6h / rate_limit 1h / 连续 transient 3 次 30min，`sources/health.py:83-89`）；**缺**：无抖动、无每日/每次请求配额头、本地 `openlist_cache` 在扫描路径未被复用（`cache.py` 的 `read_cache` 无调用方；扫描一律 `refresh=False`）、恢复器可能对仍在运行的 scan 重跑 finalizer（`source_scan_runner.py:507-566,568-603`）。

### 5.3 解决办法（批次 B5）

**第一阶段（体验与安全，不改变数据模型）**

1. **读取阶段进度语义**：改为汇报「已列出 N 个目录 / 已获取 M 个文件」的不确定态进度（前端已支持不确定进度条），并每批次写心跳；把 `stage_label` 写成有意义的当前目录；
2. **目录级 durable frontier**：新增 `source_scan_directories(scan_id, remote_path, depth, status, next_page, discovered_at, completed_at)`；发现目录即写入，每完成一页写 `next_page`，目录扫完置 `completed`；worker 从表里取 `queued/scanning` 的下一项，而不是从内存 deque 开始 → 进程重启、断电或风控中断后可以从断点续扫。SourceEvidence 写入必须保持 `(scan_id, source_key)` 幂等（UPSERT / `INSERT OR IGNORE`），避免重读一页产生重复媒体；
3. **目录/请求预算与配额**：每次扫描与每日的目录数与请求数上限（可配置，默认保守），超限时**部分成功**并提示"已扫描 X，剩余 Y 可继续"，而不是整体失败；
4. **上游 5xx 的语义**：识别为「上游存储不可用」，对该 root 冷却一段时间并给出明确提示；对 5xx/超时加**抖动**退避；
5. **复用本地缓存**：目录列举在 TTL 内优先读 `openlist_cache`，把"全量重扫"变成"只打缺失的目录"。

**第二阶段（增量语义，达成用户目标）**

6. **明确两阶段 + 硬预算**：首次必须建立基线（TXT 或一次成功的全量）；之后每轮增量固定「根目录 + 预算内最久未验证的目录」，并把 `verification_budget` 定义为**本轮允许请求的远端目录数上限**（循环内计数，达到上限即停）。同时给基线目录引入 `verification_state: unknown|verified`，`unknown` 目录进入轮转分批核验（受预算约束，不允许为了 UI 百分比一次性递归整棵目录树 —— 那会让请求量翻倍）；
7. **比对字段**：加入 size 与内容指纹（至少 size），避免 mtime 不变但内容替换漏检；
8. **增量变更集**：增量结果应显式区分 added / modified / removed / unverified，并只对变更集生成镜像/刮削任务（现状增量返回"基线克隆 + 本轮观察"的完整集合，会重复生成整批任务）。

**关于用户提出的 WebDAV 思路**

- 代码里已有 WebDAV 入口规范化（`integrations/openlist/client.py:198-199`），但当前浏览与扫描走 `/api/fs/list`；
- WebDAV `PROPFIND Depth:1` 与 `/api/fs/list` 的请求数**基本相同**（都是逐层列举），但 WebDAV 可利用 OpenList 自身的目录缓存；
- **建议**：保留 API 通道（已有 telemetry/限速/冷却治理），把"用户已有目录树"作为**零请求基线来源**（TXT 导入已支持）；OpenList 只承担增量核对。这比"用 WebDAV 抓全树再模拟"更省请求、更安全。

**验证（B5）**

- 后端：`backend/tests/test_v4_scan_closeout.py`、`test_openlist_*`、`test_v4_directory_tree_encoding.py` 等；新增"扫描失败可续扫""超预算部分成功""增量只核对变更目录"三个夹具；
- **不做大库真实全量扫描**（风控红线）；用受控夹具 + 允许用户自己在本机做小范围实测。

---

## 6. P-06 115 与百度目录树解析适配

### 6.1 真实样本实测

| 样本 | 编码 | 连接符 | 缩进 | 外壳层 |
|---|---|---|---|---|
| `docs/samples/根目录20260703203700_目录树.txt`（**115**） | UTF-16LE + BOM `FF FE` | `\|-`（首行 `\|——`） | 仅 ASCII `\|` + 空格 | `根目录` → `动画` 两层 |
| `docs/samples/01动画_文件目录.txt`（**百度**） | UTF-8 无 BOM | `├──` | `│` + 3 空格 | 无 |
| `docs/samples/新番_文件目录_20260712005344.txt`（**百度**） | UTF-8 无 BOM | `├──` | `│` + 3 空格 | 无 |

归属依据是仓库内已固定的事实：`backend/tests/test_v4_sample_corpus.py:41`、以及 V4 重构前 `34b4f2f:backend/app/sources/baidu.py:8-18` / `pan115.py:40-42`。

### 6.2 当前 V4 与旧实现的差异（关键项）

| 差异点 | 旧实现（`34b4f2f`） | 当前 V4 | 结论 |
|---|---|---|---|
| 连接符/层级基准 | 115 与百度**两套独立正则**，115 用 1 基深度、百度用 0 基 | 单函数顺序试三种正则，统一 0 基（`scanner.py:28-35,103-120`） | 基本覆盖 |
| 外壳层裁剪 | 115 固定跳过首层 `根目录` | 粘性标志 `skip_tree_root` 裁掉"栈内首个非空段"（`scanner.py:286,303-305`）；真实 115 样本实测得到 `动画/…`，**与旧实现一致，不是缺陷**。但该标志是**跨全文件的粘性状态**：任何命中 `^\|[—-]{2}(.+)$`（`:34`）或 `^(?:\| )*\|-(.+)$`（`:35`）的行都会置位它 | **脆弱点**：混入 115 风格行的百度树会被多裁一层；应按 provider 显式决定，不用粘性标志 |
| 首行外壳识别 | `pan115.py:258-260` | 生产链路读取后 BOM 已被剥离（`_decode_tree_bytes` 对 `FF FE` 选 `utf-16`），`_PAN115_ROOT_LINE.match(首行)` 实测 **True**，解析出 2472 条、顶层段为 `动画` | **原"BOM 顶掉锚点、靠巧合"结论已撤销**（见 §0.4 勘误 1） |
| `\|` + 两个短横的形态 | — | `^\|[—-]{2}`（`scanner.py:34`）会把 `\|--动画` 匹配成 115 外壳根行并置位上面的粘性标记 | **待样本确认**：真实 115 样本用的是 `\|-`，目前没有 `\|--` 的真实样本可判定影响面 |
| 视频扩展名 | 含 `.rmvb` | `VIDEO_SUFFIXES` 只有 10 个，**无 `.rmvb`**（`scanner.py:123-134`） | **缺陷**：`.rmvb` 会被当目录压栈，污染后续层级 |
| 非视频资源 | 全部产出并带 `resource_hint` | 全部丢弃（`METADATA_SUFFIXES` 声明但无引用，`scanner.py:136-137` 死代码） | 设计取舍，需确认 |
| provider 自动识别 | 按正文连接符**互斥判定**（`media_presets/service.py:796-817`） | **完全没有**，provider 只来自请求（`api/media_v4.py:583-587,1324`） | **能力回退**：用户选错来源时无内容级纠错 |
| 编码兜底 | 百度旧链路 `gbk(errors="replace")` | 全部 strict，失败即报错 | 更严格，可接受 |

### 6.3 施工方案（批次 B6）

1. **按 provider 分支语法、共享路径栈**：`parse_baidu_line()` / `parse_pan115_line()` 各自产出统一的 `TreeNode(depth, name, kind)`，再由共享的 `build_tree_relative_paths()` 建栈；**不要**用跨文件粘性 `skip_tree_root` 表达"跳过外壳根"，而是把 `根目录` 解析成 `kind="root"`，遇到就清栈跳过（百度则完全不跳过）；
2. **不做 BOM 归一改造**（原方案已撤销，见 §0.4 勘误 1）；改为用真实 115 样本做**回归锁定**：断言解析结果以 `动画/` 开头、条目数稳定（当前实测 2472 条）；
3. **恢复正文 provider 探测**：两种格式互斥判定，页面选择与探测结果不一致时给出提示（可作为非阻断警告）；
4. **扩展名表补齐并可配置**：至少补 `.rmvb`，把"视频扩展名"做成单一来源；
5. **非视频资源明确取舍**：要么带 `resource_hint` 保留（旧版行为），要么显式丢弃并写入扫描诊断（当前是静默丢弃）；
6. **回归夹具**：三个真实样本 + 合成边界（无 BOM 的 115、`|--` 变体、含半角 `:` 的条目、超大文件）；
7. 注意 `tree_root.py:92-108` 会因条目含半角 `:` 而**整库拒绝**，需与真实数据核对（全角 `：` 不受影响）。

---

## 7. P-07 外部 MPV 整合包：可行性与方案

### 7.1 现状事实

- 启动链路唯一：`api/playback_v4.py:74-87` → `media_v4/playback/session.py:103-109` → `playback/mpv.py:53-150` → `playback/mpv_runtime.py:397-470`；
- 实际传入的关键参数（行号均在 `mpv_runtime.py`）：`--config-dir=<KumiPlayer 自己的 portable_config>`（`:424`）、`--include=<kumiplayer/mpv.conf>`（`:426-429`）、`--script=<kumiplayer/scripts/*.lua>`×4（`:430-434`）、`--watch-later-directory`（`:435`）、`--demuxer-cache-dir`（`:436`）、`--log-file`（`:437`）、`--script-opts=thumbfast.thumbnail=…`（`:439`）、`--script-opts=kumiplayer_anime4k.*`（`:441-451`）、`--input-ipc-server`（`:455-456`）、`--start`（`:457`）、`--title`（`:459`）、`--force-media-title`（`:460`）、`--autocreate-playlist=no` 等（`:461-465`）；**未传** `--no-config`、`--input-conf`、`--scripts-append`；
- **没有 `--config-dir` 就等于"不读整合包配置"不成立**：`--config-dir` 是替换语义（本地 `mpv --list-options` 标记 `[not in config files] [file]`，见 [mpv 官方手册](https://mpv.io/manual/stable/)），一旦指定，mpv 不再使用 exe 同级 `portable_config` 与用户 `%APPDATA%\mpv` → **整合包的 `mpv.conf`/`input.conf`/`scripts/` 会被整体绕过**；
- 硬门控：必须存在自有配置目录与自有层（`mpv_runtime.py:247-253`）、4 个自有脚本必须齐全（`:267-277`）、必须能写 `data/mpv-state`（`:417-420`）；`mpv_path` 配置字段仍存在但播放不使用；
- 依赖自有配置的功能中，**只有三处真正依赖配置**：Anime4K（`kumiplayer_anime4k.lua:23,138-141`，且启用时会 `glsl-shaders clr` 清空他人 shader）、截图（脚本强制覆盖 `screenshot-directory/format/template`，`:65,80-82`；且整合包若已绑 F10/Alt+F10，弱绑定必输）、uosc 右键菜单（跨版本可能失效，有降级）；
- **进度、历史、已看状态、自动下一集只依赖 mpv 官方 IPC + 后端 SQLite**（`mpv_runtime.py:455-456`、`session.py:103-131`、`playback/store.py:18-138`），与谁提供配置目录无关 → "用整合包"不会伤到核心功能。

### 7.2 三种架构对比

| 方案 | 对整合包侵入 | 进度/历史/自动下一集 | 截图 | Anime4K | 成本 | 风险 |
|---|---|---|---|---|---|---|
| A 维持现状（唯一内置干净 MPV） | 无（根本不碰） | 全保住 | 全保住 | 保住 | 0 | 不满足"用整合包"诉求 |
| B 外部档 = 整合包 exe + `--config-dir=<整合包>` + `--include` 自有 mpv.conf + 自有脚本 + 状态重定向 | 中（参数不改文件，但覆盖同名项） | 全保住 | 部分失效 | 冲突（清 shader） | 中 | 中 |
| **C 外部档 = 整合包 exe，不传 `--config-dir`**（让 mpv 自发现 exe 同级 `portable_config`）+ 仅追加自有脚本与状态重定向 | **低**（不干预配置选择与脚本生态） | 全保住 | 依赖键位，可能退化为整合包行为 | 外部档不启用自有 Anime4K | 中 | 低-中 |
| D 最小插件集（只注入截图脚本 + 进度 IPC） | 最低 | 全保住 | 可能被覆盖 | 不启用 | 低 | 低，但丢功能 |

### 7.3 推荐：内置为默认 + 可选外部整合包档（外部档零注入）

1. 外部档**不传** `--config-dir`、`--include`、`--script`、`--script-opts`，只传本次会话必需参数：`--input-ipc-server`、`--start`、`--title`、`--force-media-title`、`--save-position-on-quit=no`、`--no-resume-playback`、`--autocreate-playlist=no` + 受控播放列表；
2. 这样 mpv 自行发现并加载整合包的 `portable_config`（`mpv.conf` / `input.conf` / `scripts` / `shaders` / uosc），KumiPlayer 完全不碰该目录，也不修改其中任何文件；
3. **外部档不加载任何 KumiPlayer Lua**：进度、历史、已看状态、自动下一集全部由后端 IPC（`media_v4/playback/session.py:571-642`）与 SQLite 完成，与 Lua 无关；Anime4K 与截图脚本属于**会改变整合包行为**的插件（前者 `glsl-shaders clr`，后者覆盖 `screenshot-directory/format/template` 并占用 F10/Alt+F10），外部档一律不注入；
4. 若将来仍希望外部档提供 KumiPlayer 截图/Anime4K，应做成**默认关闭的可选开关**，并在开关旁明确说明会接管对应行为；
5. 若确实必须注入脚本参数，必须用 `--script-opts-add`（本地 `mpv --list-options` 存在 `-add/-append/-set/-clr/-del/-remove` 变体，说明基础形式是整体设置，直接写会覆盖整合包同名键）；
6. 播放前只做"路径存在 + 可执行"的轻校验（不做内置档的 SHA-256 / 清单门控）；配置模型建议 `player_mode: internal|external` + `external_mpv_path`，旧 `mpv_path` 保留一次迁移；
7. 清理死代码：`playback/mpv.py:205-218`（无人调用的旧契约检查）、`mpv.py:233` 的过期脚本路径；
8. 设置页与首次引导文案改为「播放模式：内置播放器 / 外部 MPV 整合包」，并说明"KumiPlayer 不会修改该目录中的配置或脚本"（`src/pages/FirstRunSetup.tsx:152-162`、`src/pages/SettingsPage.tsx:587-608`、`src/api/config.ts:114`、`api/config.py:398-407,410-429`）。

**合规边界**：加载用户**本地自备**的整合包不等于"从第三方整合包复制配置"（`AGENTS.md` 禁止的是后者）；但不能把该整合包或其组件打进安装包（未补齐许可证与发布材料前只能本地开发）。

**待确认**：`--config-dir`/`--input-conf` 的替换语义、`--script-opts` 的合并语义建议在落地前用 `mpv --list-options` 与本机 v0.41.0 实测复核（本轮只核到了选项存在性与变体）。

---

## 8. 施工批次建议

| 批次 | 内容 | 涉及层 | 依赖 |
|---|---|---|---|
| **B1** | **B1-1（已实现）** 图片产物缺失止血：`scrape.py` 写 `reason_code="artifact_incomplete"` + `service.py` 专用文案「媒体资料已获取，但部分图片下载或发布失败。」+ 前端按钮改为「重新下载媒体图片」；**B1-2** metadata/artifact 状态解耦 + 独立 `retry_artifacts` + 媒体墙可见性 | 后端为主 + 少量前端 | 无（最高优先） |
| **B3** | 识别门控纠偏（needs_review 与身份解耦 + issue severity）、SP 容器继承、前置画质前缀清洗 | 后端 + 前端 | 无 |
| **B6** | 115/百度解析分支化（provider-aware 语法 + 共享栈）、`.rmvb`、正文 provider 探测、真实格式夹具 | 后端 | 在 OpenList/TXT 基线共用之前 |
| **B5** | OpenList 目录级 frontier、请求硬预算、断点续扫、`verification_state` 分批核验、direct_listing_hash | 后端 | 依赖 B6（基线格式先稳定） |
| **B2** | 伪父系列修复 + 历史行只读收口（来源卡 11 项归零） | 后端 | 无（不阻塞主链，可并行） |
| **B4** | 弹窗/表单基础层、命令区优先级冲突、进度行重排、Portal 作用域治理 | 前端 | 等 B1/B3/B5 的状态词与按钮稳定后统一做；**三按钮竖排这一条可以提前单独热修** |
| **B7** | 外部 MPV 整合包档（外部档零注入） | 后端 + 前端设置 | 需用户先确认 §9 决策点 2 |

**关键依赖链**：`B3 识别门控 → B6 输入基线 → B5 OpenList 增量`。原先把 B5 排在 B1/B2 之后没有真实代码依赖；OpenList 的实际前置是"识别语义与 TXT 基线格式先稳定"。

每批要求：先补针对性失败测试 → 最小实现 → 一次集中审核；每批独立提交（Conventional Commits 中文格式）。**不得**在施工过程中修改用户真实库数据（真实错误数据是诊断证据，按项目硬规范原样保留）。

---

## 9. 需要用户决策的点

1. **图片缺失是否允许作品进入媒体墙？（已定：允许）** Provider identity 与 metadata 已成立的作品，不因 `poster.jpg` 下载失败而从媒体库消失；用占位图 + 非阻塞提示。当前 `library_v4.py:170-172` 的 ready 过滤要在 B1-2 修正。
2. **是否允许修改 `docs/PROJECT_MEMORY.md` 中与外部 MPV 冲突的旧条目？（已定：允许，B7 时改）** 新产品决策是"内置档 + 外部整合包档并存，且外部档零注入"；旧的"只读自己的 config-dir / 不读外部整合包"规则在 **B7 开工时**更新即可，不阻塞 B1–B6，现在不动这个文件。
3. **OpenList 预算耗尽后的语义（已定）**：**"部分进度可持久化"接受，"部分扫描算完整成功"不接受。** 首次 Full Baseline 预算耗尽 → `paused / resumable`，frontier、evidence、游标全部保留，**不得 confirmed**，继续扫描直到 frontier 为空才算建立基线；已有基线的增量 → 预算内目录的新增/变化可安全落库，未验证目录保持 `pending verification`，来源**不得宣称"已全部检查完毕"**。
4. **`proxy_url` 长期指向 `127.0.0.1:7890`**：该端口在多数时间未监听（本轮推送也一度因此失败，用户开启代理后恢复）。请确认是"平时长期开代理"还是"清空该配置"。无论哪种，都建议补一个**图片源连通性测试**与**代理错误的明确分类**（代理不可达 / DNS / 超时 / HTTP 状态码），否则同类问题会反复以"在线资料服务不可用"的形式出现。

---

## 10. 未验证项与风险（必须与结论一起读）

1. 未对 TMDB / `image.tmdb.org` / OpenList 发起任何新请求，因此**图片下载失败的具体 HTTP 原因**（超时 / 429 / 代理不可达 / 404）未定论；本文的"死代理"判断基于本机端口探测与配置值，属高置信推断。
2. 前端缺陷的**几何级重叠**（而非 2-6px 贴合）与弹窗 X 是否真换行，需运行态 devtools 核对 computed style；`node_modules` 当前缺失，未运行 vitest。
3. OpenList 244 秒的耗时与 1 req/s 的量级为静态推断 + 数据库时间戳实测，未做真机大库复现（风控红线）。
4. 未运行全量 `pytest`、未构建、未打包。
5. 本轮未修改任何源码、配置或用户数据；只新增本文档与 `.gitignore` 的审阅目录例外。
6. **本文件已在 §0.4 记录第二轮勘误**：原"115 BOM 导致锚点失效"、"OpenList 增量仍核对所有目录"、"retry 是确定性空操作"、"图 1 现象全部归因于图片"四处结论已更正。引用本文件时请以勘误后的版本为准。
7. 图 7 只被当作"用户希望使用现成 MPV 整合包"的说明；**画面中那个绿色矩形选框本身没有被分析**——如果它是在报告某个 MPV 画面/OSD/插件故障，需要用户补充说明后单独排查。

---

## 11. 附：本轮取证的直接数据来源（便于复核）

- 只读 SQLite：`works`（41 active + 192 superseded）、`scrape_bindings`（5 条非 confirmed）、`artifacts`（poster 缺失）、`source_scans`（夸克 2×244s 失败）、`revision_issues`（11 条 `unresolved_parent_relation`、1 条 `parsed_facts_need_review`）、`source_roots`（9 个来源根）。
- 本地 MPV 选项清单：`third_party/mpv/runtime/mpv.exe --list-options`（v0.41.0，1213 行）。
- 真实目录树样本：`docs/samples/` 三个文件。
- 真实 `data/mirror` 目录产物核对（poster/fanart/clearlogo/movie.nfo 存在性）。

---

## 12. 施工进度

> 本节记录第一批（用户报的 7 个问题，B1–B7）。第二批"底层逻辑审查与优化"（O1–O11，含第二轮并行只读审查与逐条复核）在 **§13**，两批均已完成并逐条留证。

### B1-1 图片产物缺失止血（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/jobs/scrape.py` | 完整性门控失败时写入 `reason_code="artifact_incomplete"`（原先只有 `reason` + `completeness`） |
| `backend/app/media_v4/revisions/service.py` | `_friendly_metadata_reason()` 增加该原因码专用文案「媒体资料已获取，但部分图片下载或发布失败。」 |
| `src/components/media/V4ExecutionProgress.tsx` | 新增 `retryMetadataLabel()`：`metadata_reason_code === 'artifact_incomplete'` 时按钮为「重新下载媒体图片」（进行中「正在重新下载…」），两处渲染点共用 |
| `backend/tests/test_v4_scrape_pipeline.py` | 新增 `test_completeness_failure_records_artifact_incomplete_reason_code`：资料 ready、图片地址为空 → 断言 `failed` + `artifact_incomplete` + 文案含「图片」+ action 为 `retry_metadata` |
| `backend/tests/test_v4_work_execution_detail.py` | 恢复策略参数化用例新增 `artifact_incomplete` 分支 |
| `tests/components/media-management-execution.test.tsx` | 新增用例：图片原因码时按钮为「重新下载媒体图片」，且不再出现「重新获取媒体信息」 |

验证（先红后绿）：

- 实现前：两个后端用例失败（`metadata_reason_code` 为空、恢复文案不含「图片」）。
- 实现后：`backend/tests/test_v4_work_execution_detail.py` + `test_v4_scrape_pipeline.py` **37 项通过**；`-k "v4 or scrape or metadata or completeness or artifact"` 子集全部通过。
- `tests/components/media-management-execution.test.tsx` **15 项通过**（含新增用例）。
- `npm run test:contracts`：**89 项中 88 通过**；唯一失败为 `tests/media-management-ui-contract.test.ts:30`「来源卡恢复为固定方形 GridView 项目，并保留小窗口单列回退」，该正则断言在 **HEAD 版 `src/index.css` 上同样不匹配**（已用 `git show HEAD:src/index.css` 复现），属既有失败，与本次改动无关。

### B1-2 metadata / artifact 状态解耦（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/jobs/completeness.py` | 新增 `is_artwork_reason()` / `artwork_only_reasons()` / `artifact_only_failure()`：只有**全部**原因是图片问题才算产物降级 |
| `backend/app/media_v4/jobs/scrape.py` | 完整性失败分流：纯图片原因 → `metadata_state` 保持 `ready` + `artifact_state="degraded"`；缺 Work NFO / 剧集 NFO / 无镜像等物化失败仍判 `failed`。新增 `retry_artifacts()`：复用已保存的 `metadata_json` 重发产物，**不产生任何在线请求** |
| `backend/app/media_v4/projection/library.py` | 投影复查不再因缺图把作品降成 `failed`（作品不再从媒体墙消失）；缺 NFO 等仍按原判处理 |
| `backend/app/media_v4/revisions/service.py` | 新增 `_artifact_view()`：对历史"只缺图片"的 `failed` 记录做**只读归一**（不改真实数据）；工作单元与执行详情新增 `artifact_state` / `artifact_reasons` |
| `backend/app/api/media_v4.py` | 新增 `POST /api/v4/metadata/artifacts/retry`（只重下图片，不重跑搜索） |
| `src/api/mediaV4.ts`、`src/components/media/V4ExecutionProgress.tsx`、`src/pages/MediaManagementPage.tsx` | 增加 `artifact_state` 类型与 `metadataArtifactsRetry`；缺图作品自动展开并显示中性提示「部分图片未下载成功，可重新下载；不影响浏览和播放。」+「重新下载媒体图片」按钮 |

验证：

- 新增后端用例 3 条：`test_missing_artwork_keeps_metadata_ready_and_marks_artifacts_degraded`、`test_legacy_artwork_only_failure_reads_as_ready_without_rewriting_data`（同时断言数据库里的历史失败记录未被改写）、`test_retry_artifacts_republishes_without_asking_provider_again`（断言复用已保存的 provider 身份，即本轮没有在线搜索）。
- 按新决策更新 2 条旧契约：`test_missing_local_artwork_keeps_metadata_ready_and_marks_artifacts_degraded`（原 `test_metadata_ready_requires_poster_fanart_work_nfo_and_episode_nfo`）、`test_manual_confirm_publishes_work_when_only_artwork_is_missing`（原 `..._must_not_publish`；新用例额外断言原因**全部**是图片原因，防止把物化失败也放行）。
- `pytest -k "v4 or scrape or metadata or completeness or artifact or projection or library"` **全绿**；全量 `backend/tests` 仅 **4 条既有失败**（`test_settings_outline_navigation_ui.py` 3 条、`test_desktop_window_responsive_layout.py` 1 条），全部断言 `src/pages/FirstRunSetup.tsx` / `src/pages/SettingsPage.tsx` / `src/index.css`，这些文件本次未改动。
- 前端：`tests/components/media-management-execution.test.tsx` **16 项通过**；`npx tsc -b` **exit 0**；`npm run test:contracts` **89 项中 88 通过**（唯一失败与 B1-1 相同，并已在 HEAD 上复现为既有失败）。

### B3 识别门控分级 + 前置画质前缀（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/parsing/parser.py` | 结构系列身份已确定时不再保留 `needs_review`（根目录直属的 `SP01.mkv` 不再阻断整批导入） |
| `backend/app/media_v4/resolution/resolver.py` | `needs_review` 分级：身份可解析 → `parsed_facts_review_hint`（提示）；身份不可解析 → `parsed_facts_need_review`（阻断）。新增 `NON_BLOCKING_ISSUE_CODES` |
| `backend/app/api/media_v4.py` | 预览响应新增 `blocking_issue_count`（提示类 issue 不计入） |
| `backend/app/recognition/title_cleaner.py` | 新增 `strip_leading_quality_prefix()` 并在容器清洗第 0 步应用：剥离 `4k/8k/2160p/BDRip…` 等前置画质；只匹配已知画质词且要求后接分隔符或中日韩字符，不误伤 `86`、`22/7`、`3月的狮子`、`91Days` |
| `src/api/mediaV4.ts`、`src/pages/MediaManagementPage.tsx` | 前端改用 `blocking_issue_count` 决定确认可用性；仅有提示类 issue 时显示 info 而不是 warning |

验证：

- 新增 `backend/tests/test_v4_recognition_gating.py` 4 条用例：前缀清洗正例/负例、身份可得 → 提示类、身份不可得 → 阻断类。
- 更新 `backend/tests/test_v4_episode_assets.py::test_parser_review_flag_becomes_a_resolution_issue`：仍断言标记产生 issue，但按新分级断言为提示类。
- 全量 `backend/tests`：仅剩 **4 条既有失败**（settings / first-run 源文件断言，涉及文件本次未改动）。
- 前端：`npx tsc -b` **exit 0**；`vitest run` 24 个文件、**193 项中 192 通过**；唯一失败 `media-management-p005-rework.test.tsx::来源卡使用紧凑的响应式任务卡布局` 断言的是 `src/index.css` 的 `repeat(auto-fill, minmax(270px, 300px))`，与已知既有失败同源（`index.css` 本次未改动）。

### B6 115/百度目录树解析适配（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/scanner.py` | ① `VIDEO_SUFFIXES` 补 `.rmvb`、`.rm`、`.mts`、`.mpg`、`.mpeg`（旧链路有 `.rmvb`，V4 曾漏掉，会被当目录压栈污染后续条目）；② 新增 `detect_tree_provider()`：按正文语法返回 `pan115 / baidu / unknown / ambiguous`；③ 外壳根裁剪改为 `_skip_shell_root(provider, detected)`：**正文语法优先**——内容能唯一判定格式时按正文解析（用户选错来源标签也不会解析错），只有正文无法判定（空文件/纯路径清单）才退回来源标签；百度树永不被裁层。彻底去掉"任何一行命中 115 语法就把整棵树裁一层"的跨文件粘性状态；④ `tree_media_relative_paths()` / `_tree_relative_paths()` 增加 `provider` 关键字参数，`build_directory_tree_evidence()` 透传 provider |

验证：

- 新增 `backend/tests/test_v4_tree_provider_adaptation.py` 8 条：`.rmvb` 必须识别为视频且不污染后续同级条目；115 正文在 `provider="pan115"` 与 `provider="baidu"` 下**都**裁外壳得到 `动画/Show/...`（正文优先，覆盖"用户选错来源"场景）、百度正文写成 115 标签也不会被凭空裁层；`detect_tree_provider` 四种取值 + 混合语法 `ambiguous`；真实 115 样本（`docs/samples/`，不在仓库时自动 skip）顶层段必须只有 `动画`。
- **回归修正过程**：初次实现采用"来源标签优先"，导致 `test_v4_sample_corpus.py` 7 条真实样本合同失败——该文件的夹具把 115 样本也标成 `provider="baidu"`。用 `git stash` 二分确认是本次改动引入后，改为**正文语法优先**：语料合同与新增用例同时通过。这也说明"标签优先"会让选错来源的用户解析错误，正文优先才是正确设计。
- 定向 `test_v4_tree_provider_adaptation.py` + `test_v4_sample_corpus.py` 全绿；全量 `backend/tests` 仅剩 4 条既有失败。

未做（有意保留）：正文探测结果目前只用于选择解析规则，**还没有接到扫描诊断/UI 提示**。用户在来源选择里选错 provider 时，解析已按实际格式工作，但界面不会提示"检测到该目录树更符合 115 格式"。

### B5-1 读取阶段进度语义（2026-09-15，已实现并验证）

先做 B5 里不涉及数据库结构的一半：让"卡住不动"变成"能看到在动"。

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/durable_scan.py` | `get_durable_scan()` 在 `stage=reading_source` 且总量未知时，把 `stage_label` 派生为「正在读取媒体来源（已发现 N 个媒体文件）」；响应新增 `discovered_count`（读取阶段实时计数，来自已落库的证据数） |
| `src/api/mediaV4.ts`、`src/pages/MediaManagementPage.tsx` | 扫描状态类型新增 `discovered_count`；总量未知时进度行显示「已发现 N 个媒体文件」，取代原先的「正在建立来源清单…」占位（**不给假百分比**，条形仍是不确定态） |

验证：

- 新增 `backend/tests/test_v4_read_progress_semantics.py` 3 条：读取阶段 143/0 → `progress=None`、`discovered_count=143`、label 含计数；已知总量 5/10 → 保留静态 label 与 `progress=0.5`；刚进入读取阶段 0/0 → 仍是静态 label（不显示"已发现 0 个"）。
- 全量 `backend/tests` 仅剩 4 条既有失败；`npx tsc -b` **exit 0**；组件测试结果见下一条记录。

未做：
- **没有**为该进度行新增前端组件用例（该行位于导入流程内部，现有测试没有覆盖这条渲染路径）；改动由 `tsc` 与现有组件套件回归保证。
- B5-2 的内容仍未实现：目录级 durable frontier、请求硬预算、预算耗尽的 `paused / resumable`、增量 `verification_state: unknown→verified`。

### B5-2a 目录级 frontier（schema v19）（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/persistence/schema_v4.py` | `V4_SCHEMA_VERSION` 18→19；新增 `create_v19_structures()`（表 `source_scan_directories`：`scan_id, remote_path, depth, status, next_page, discovered_at, completed_at` + 待处理索引）与 `migrate_schema_v18_to_v19()` |
| `backend/app/media_v4/persistence/database.py` | `PRAGMA user_version` 字面量同步为 `19`；新增 **`if version == 18` 显式迁移分支**（缺这条会让既有 v18 真实库落到 `< CURRENT` 分支被判"旧架构需要一次性重置"）；空库创建路径补 `create_v19_structures`；`REQUIRED_TABLES` 与内存期望结构同步加入新表 |
| `backend/app/media_v4/sources/scan_frontier.py`（新增） | frontier 读写原语：`ensure_directories`（幂等，**不重置已完成/进行中的行**）、`next_pending_directory`（浅层优先，`BEGIN IMMEDIATE` 占用）、`mark_directory`（写回 `next_page` / `completed_at`）、`directory_counts`、`clear` |

验证：

- 新增 `backend/tests/test_v4_scan_frontier.py` 4 条：登记幂等且不把已完成目录重新排队（否则"续扫"退化成重扫）、取用顺序浅层优先、被中断的 `scanning` 目录仍算待处理并从记录的 `next_page` 继续、计数按 `scan_id` 隔离且 `clear` 只清自己。
- `backend/tests/test_v4_schema_migration.py` 新增 v18→v19 用例：构造物理 v18 库（并插入真实来源与扫描记录）→ `initialize()` → 断言 `user_version == 19`、新表存在、**既有来源与扫描记录未被改写**。
- 同步更新 `backend/tests/test_v4_schema_contract.py::test_future_database_version_is_rejected` 的版本字面量（`18→19`、未来版本 `19→20`）；该用例本身就要求 schema 升级时同步断言，否则会静默失去"未来版本被拒绝"的语义。
- 全量 `backend/tests` 结果见下一条记录。

未做：本步只建表与读写原语，**尚未**接入 OpenList 读取循环（B5-2b 才做），也还没有请求硬预算与 `paused / resumable` 语义。

### B5-2b-1 OpenList 读取接入 frontier（断点续扫）（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/scanner.py` | `scan_openlist_directory()` 新增可选 `frontier_next / frontier_mark / frontier_add` 回调；传入时遍历由 frontier 驱动（不再用内存 deque）：每列完一页写回 `next_page`，目录完成置 `completed`，新发现子目录入队 `queued`；不传时保持原内存队列行为（旧端点与既有测试完全不变） |
| `backend/app/media_v4/sources/scan_handlers.py` | `openlist_full` 处理器注入绑定到 `task.scan_id` 的 frontier 回调；**只有扫描完整走完才 `clear`**，异常路径保留断点供续扫 |
| `backend/tests/test_v4_openlist_frontier_resume.py`（新增） | 3 条断点续扫用例 |

**过程中发现并修掉的真实缺陷（关键）**：断点边界必须**先交付已收集证据、再推进游标**。第一版实现里，目录被标记 `completed`（或页游标推进）时，仍挂在内存 `pending` 缓冲里的证据会随中断丢失，而续扫又不会重列该目录/该页——等于**静默丢媒体**。现在在"翻页前"与"目录完成前"两个断点各 flush 一次，并由测试锁定"已标记完成的目录，其证据一定已交付"。

验证：

- 新增 `test_v4_openlist_frontier_resume.py` 3 条：① 在 `ShowB` 上失败 → 断点状态为"根目录与 ShowA 已完成、ShowB 待处理、**ShowA 证据已交付**"；换健康客户端续扫 → **只重列 ShowB**，两轮交付合起来才是完整证据集；② 150 条目的目录在第 2 页失败 → 第 1 页 100 条已交付、断点 `next_page=2`，续扫直接请求第 2 页且不重列第 1 页；③ 完整走完并清理后再扫一次，仍是完整的三次目录请求（防止"清理逻辑写错导致每次全量重列"）。
- 全量 `backend/tests` 仅剩 4 条既有失败。

未做：请求硬预算与预算耗尽的 `paused / resumable` 语义（B5-2b-2）。因此**目前还没有"部分完成"这条状态**；本步只保证"中断之后能接着扫"，不保证"扫不完时界面说实话"。

### B5-2b-2 请求硬预算与 paused / resumable（2026-09-15，已实现并验证）

严格按已确认的语义实现：**"部分进度可持久化"接受，"部分扫描算完整成功"不接受。**

| 文件 | 改动 |
|---|---|
| `sources/scanner.py` | 新增 `SourceScanPaused` 与 `DEFAULT_FULL_SCAN_DIRECTORY_BUDGET = 400`；`scan_openlist_directory()` 支持 `directory_budget` / `scan_stats`：达到预算时**干净停下**（不抛异常），把 `budget_exhausted / directories_listed / evidence_count` 写进 `scan_stats`，未列目录留在 frontier |
| `sources/scan_handlers.py` | `openlist_full` 传入预算与 stats；`budget_exhausted` 时抛 `SourceScanPaused`（位置在 `_assert_scan_identity` 与 `clear` **之前**）→ 断点保留；预算可由请求参数 `directory_budget` 覆盖 |
| `sources/source_scan_runner.py` | 新增 `except SourceScanPaused` → `_finish_scan(status="paused")`；恢复路径同样处理 |
| `sources/durable_scan.py` | `status=paused` 分支 + 标签「本次巡检已达请求预算，可继续扫描」；响应新增 `resumable`（仅 paused 为 true） |
| `projection/source_libraries.py` | paused 计入 `attention_count` 与 `can_resume`，并把暂停原因显示为卡片文案 |
| `api/media_v4.py` | `SourceScanRequest` 新增 `resume_scan_id`；全量分支在 `resume_scan_id` 属于**同一 root 且状态为 paused** 时复用该 scan_id（否则 409），frontier 才能真正续扫 |

验证：

- `test_v4_openlist_frontier_resume.py` 新增：预算=1 → 只列根目录、`budget_exhausted=True`、frontier `completed=1 / pending=2`；提高预算续扫 → **只列剩余两个子目录**、pending 归零。
- `test_v4_read_progress_semantics.py` 新增：paused → `resumable=True`、标签含「继续」、`interrupted=False`；completed → `resumable=False`、标签「识别结果已就绪」。
- 全量 `backend/tests` 仅剩 4 条既有失败。

未做 / 未验证：

- **前端「继续扫描」入口尚未接入**（B5-2b-3）：后端已支持 `resume_scan_id`，界面目前只能看到「本次巡检已达请求预算，可继续扫描」的提示。
- `resume_scan_id` 的 409 校验路径**没有自动化测试覆盖**（需要 OpenList API 上下文的 mock）。本轮只完成代码实现与人工审查，这一点如实记录，不当作已验证。

### B5-2b-3 前端「继续扫描」入口（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `src/api/mediaV4.ts` | `startDurableScan` 请求新增 `resume_scan_id`；扫描状态响应新增 `resumable` |
| `src/pages/MediaManagementPage.tsx` | 来源卡主按钮在 `scan.status === 'paused'` 时显示「继续扫描」；点击后用**同一 scan_id** 调 `startDurableScan({ resume_scan_id })` 再进入原有轮询流程；轮询新增防死循环保护（提交续扫后容忍 5 次"仍暂停"，之后提示「已达请求预算，可稍后继续扫描」） |
| `tests/components/media-management-overview.test.tsx` | 新增用例：paused 来源卡显示「继续扫描」，点击后断言 `startDurableScan` 收到 `resume_scan_id: 'scan-paused'` 与 `scan_mode: 'full'` |

**过程中发现并修掉的隐患**：把 `paused` 变成一种来源卡状态后，原有的轮询循环没有 `paused` 终止条件——卡片会**永远转圈**（既不 completed 也不 failed）。现在有上限容忍 + 明确文案，测试与 `tsc` 通过。这是引入新状态时必然出现的连带问题，不在原施工文档里。

验证：`npx tsc -b` **exit 0**；`vitest run` 24 个文件、**194 项中 193 通过**（唯一失败仍是既有 CSS 断言）；`npm run test:contracts` **89 项中 88 通过**（同一既有失败）。

至此 B5 主体收口：读取阶段可见计数（B5-1）、目录级 frontier（B5-2a）、断点续扫（B5-2b-1）、请求预算与暂停语义（B5-2b-2）、前端续扫入口（B5-2b-3）。**剩下 B5-2c：增量 `verification_state: unknown→verified`**（改增量 state JSON，无 schema 变更），随后进入 B2 / B4 / B7。

### B5-2c 增量 verification_state 与目录清单哈希（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/incremental.py` | 增量 state 的每个目录新增 `verification_state`（`unknown` / `verified`）与 `listing_hash`：**TXT 基线产物一律 `unknown`**（它只证明"这些目录存在过"），完整扫描产物为 `verified`，每轮真正列过的目录写回 `verified` + 当前 mtime + 清单哈希；新增 `_listing_hash()`（对直接子项的 `name/is_dir/size/modified` 求稳定哈希），**哈希变化同样判定为"目录发生变化"**，解决"网盘 mtime 没变但内容变了"的漏检；`scan_stats` 新增 `verified_directories` / `unknown_directories` |
| `src/api/mediaV4.ts`、`src/pages/MediaManagementPage.tsx` | 增量提示补充「还有 N 个目录尚未远端核实，会在后续增量中逐个补齐」／「全部目录已完成远端核实」 |

验证：

- 更新 `test_v4_openlist_incremental.py` 的 stats 断言（新增两个键）并新增 2 条：TXT 基线后所有目录都是 `unknown` 且无哈希；已核对目录变 `verified` 且写入哈希。
- 新增「mtime 全不变、只多一个子目录」用例：靠清单哈希仍然发现变化并下钻。**测试过程本身是一次验证**——该断言最初以 `KeyError: /Anime/Show999` 失败，恰好证明"变化被检测到、并且真的下钻到了新目录"（我给假客户端补上该目录页面后才通过）。
- 全量 `backend/tests` 仅剩 4 条既有失败；`npx tsc -b` **exit 0**；组件 194 项中 **193 通过**（唯一失败仍是既有 CSS 断言）。

B5 至此**全部收口**（B5-1 / B5-2a / B5-2b-1 / B5-2b-2 / B5-2b-3 / B5-2c）。下一批：**B2 伪父系列与历史行只读收口**，随后 B4（前端弹窗/命令区/进度行）与 B7（外部 MPV 零注入档）。

### B2 伪父系列修复与历史行只读收口（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/resolution/resolver.py` | `_relation_work_key_from_row()`：把「父类型被 movie 关系规则**强制**收口成 tv」并入自名抑制条件。原判断要求 `media_type == child_media_type`，而强制收口后二者必然不同 → 抑制失效 → 生成 `series:<作品自身标题>:tv` 的伪父系列。**同时保留**「显式 `relation_media_type` 声明不同父类型」的情况（同名电影确实有 TV 主系列时仍建立关系） |
| `backend/app/media_v4/projection/source_libraries.py` | 新增 `_relation_parent_is_self()`：历史行里父键的标题部分等于子作品 `identity_key` 的标题部分 → 判为伪自关联，`_relation_is_pending()` 返回 False。**只读重估、不改真实数据**；父键从右侧拆类型，兼容含冒号的作品名（`福音战士新剧场版：序`） |

验证：

- 新增 `backend/tests/test_v4_relation_pseudo_parent.py` 8 条：四种 `relation_type` 的同名电影都不再生成父键；显式 `relation_media_type=tv` 的同名电影仍生成 `series:同名作品:tv`；真实外传（名称不同）仍收口为 TV 父系列；历史伪父系列（含冒号标题）判为非待补；真实缺失父系列仍计为待补；无法解析的历史消息保持原判。
- **一次「不改测试」的判断**：初次实现把自名抑制提前为无条件执行，打挂了既有 `test_same_title_movie_can_still_relate_to_tv_parent`。核实后确认那条测试锁的是**真实**语义（同名电影确实可能有 TV 主系列），于是改为精确条件（强制收口算自名、显式声明不算），两条契约同时成立——而不是把测试改掉了事。
- **一条被主动撤掉的用例**：我原想断言「`series_group` 带发布后缀时也应抑制自关联」。实测归一化不会去掉年份/发布后缀，而真实库里 11 条伪父键都是**精确等于标题**的形式；为无法证实的场景发明模糊匹配，可能把「《Show》→《Show (2020)》」这类真实父子关系误判成自关联，代价更大。因此撤掉该断言，并把判断依据写进测试注释。
- 全量 `backend/tests` 仅剩 4 条既有失败。

下一批：**B4**（前端弹窗/表单基础层、命令区优先级冲突、进度行、Portal 作用域），随后 **B7**（外部 MPV 零注入档）。

### B4 前端弹窗 / 命令区 / 进度行（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `src/index.css` | ① 命令区通用规则改为 `.media-v4-command-row > div:not(.media-v4-command-buttons)`，**修掉「三个按钮竖排且只有 2px 间距」**——此前 `.media-v4-command-row > div`（特异性 0,1,1）击败 `.media-v4-command-buttons`（0,1,0）；② 识别弹窗为 `.fui-Field` 补 `display:grid; row-gap:6px`、label/hint `margin:0`、Input/Select `min-height:38px`，并给 `.fui-DialogTitle` 固定 `minmax(0,1fr) auto` 两列 → **关闭按钮与标题同行**；③ 来源卡重命名弹窗底色由半透明 `--surface-raised` 改为实体 `--surface-solid`，并套用同一套表单节奏；④ 扫描进度行让 `.media-v4-inline-scan-status .fui-MessageBarBody` 变单列、进度网格占满宽度 → 三条基线成组 |
| `src/components/media/V4RecognitionSummary.tsx` | 作品标题输入框的 placeholder 由重复的「作品标题」改为示例「例如：偶像大师 灰姑娘女孩 U149」 |
| `tests/media-flow-dialog-layout.test.ts`（新增） | 5 条样式合同：命令区选择器排除按钮容器且按钮容器仍 `flex/wrap`；弹窗表单节奏；标题行两列；来源卡弹窗实体底色 + 节奏；进度行脱离 MessageBar 多列网格 |

验证：新契约 **5/5 通过**；`npm run test:contracts` **94 项中 93 通过**；`npx tsc -b` **exit 0**；组件 **194 项中 193 通过**。

**未决（需要用户拍板的设计口径冲突，本轮刻意不动）**：另有两条**既有失败**断言「来源卡应为方形 GridView」——`.media-v4-source-library-grid` 用 `repeat(auto-fill, minmax(270px, 300px))`、`.media-v4-library-source-card` 用 `aspect-ratio: 1 / 1` 且小窗口回退单列。当前实现是紧凑横向卡片（与「来源卡只保留来源摘要和 Fluent 操作按钮」等较新测试一致）。这属于**设计口径冲突而不是本轮报告的缺陷**，所以没有擅自把界面改回方形网格；确认以哪一版为准后再单独收口这两条断言。

下一批：**B7**（外部 MPV 整合包档：零注入 + 播放模式设置），届时按约定更新 `docs/PROJECT_MEMORY.md` 中与外部整合包冲突的旧 MPV 规则。

### B7-1 外部 MPV 整合包档：后端零注入（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `backend/app/core/config.py` | 新增 `player_mode: "internal" \| "external"`（默认 `internal`）与 `external_mpv_path`；**不自动切换模式**，旧 `mpv_path` 只作读取期兼容值 |
| `backend/app/playback/mpv_runtime.py` | `build_mpv_playback_args(..., external=False)`：external 分支**只**输出会话参数（`--input-ipc-server` / `--start` / `--title` / `--force-media-title` / `--save-position-on-quit=no` / `--no-resume-playback` / `--autocreate-playlist=no` / 窗口行为 / 受控播放列表），**完全不出现** `--config-dir`、`--include`、`--script`、`--script-opts` |
| `backend/app/playback/mpv.py` | 新增 `_resolve_player_executable()`：按 `player_mode` 解析可执行文件；外部档路径缺失或不存在时**明确报错**，不静默回退内置播放器（否则用户以为在用整合包、实际不是）；完整性门控只作用于内置运行时；两个参数构造函数透传 `external` |
| `backend/app/api/config.py` | `ConfigPatch` 接受 `player_mode` / `external_mpv_path` 并校验 `player_mode ∈ {internal, external}`；`to_public_dict()` 基于 `asdict` 自动带出新字段 |

验证：新增 `backend/tests/test_mpv_external_mode.py` **6 条**——外部档参数中没有任何 config/script 注入、会话必需参数齐全、播放列表顺序保持；内置档仍注入 `--config-dir`；外部档路径无效时报明确错误；外部档使用配置路径；旧 `mpv_path` 可作为外部路径复用；内置档忽略外部路径。全量 `backend/tests` 仅剩 4 条既有失败。

未做（B7-2）：设置页/首次引导的「播放模式」UI 与外部路径选择器；以及 `docs/PROJECT_MEMORY.md` 中与"用户自选整合包"冲突的旧 MPV 规则更新。

### B7-2 播放模式 UI 与项目记忆同步（2026-09-15，已实现并验证）

| 文件 | 改动 |
|---|---|
| `src/api/config.ts` | `PublicConfig` 新增 `player_mode` 与 `external_mpv_path`（`patchConfig` 走 `Partial<PublicConfig>`，因此保存立即生效） |
| `src/pages/PlayerTuningPage.tsx` | 新增「播放模式」区：内置播放器 / 外部 MPV 整合包；外部档展开「整合包 MPV 可执行文件」输入框 + 「选择」（`pickFile`，路径可手填），并按模式给出说明文案——外部档明确写「KumiPlayer 不会修改该目录中的配置或脚本…退出本应用后，该整合包仍然是原来的样子」 |
| `src/index.css` | `.player-tuning-fields-wide` 单列 + `.player-tuning-path-row`（长 Windows 路径可完整显示、按钮不被压窄） |
| `docs/PROJECT_MEMORY.md` | 按施工前约定更新冲突的 MPV 规则：`internal` / `external` 双档说明；脚本路径由错误的 `portable_config/scripts`（"自动加载"）改为真实的 `kumiplayer/scripts`（`--script=` 显式注入）；旧 `mpv_path` 定位改为"仅作 `external_mpv_path` 的回退值，不自行切换模式"；截图脚本同理，并注明外部档**故意不注入**自有脚本与 Anime4K |
| `AGENTS.md` | schema 版本说明由 18 更正为 **19**（B5-2a 已升级，文档不应继续误导） |
| `docs/03_运行时与发布/MPV/MPV运行时演进记录.md` | 追加一条外部整合包档记录（含未做真机验证的说明） |

验证：新增 `tests/components/player-tuning-mode.test.tsx` **3 条**（内置档不显示路径输入并说明无需准备；外部档显示路径、承诺不改整合包目录、保存时写入 `player_mode` + `external_mpv_path`；可通过文件选择器指定 `mpv.exe`）。全量组件 **197 项中 196 通过**、契约 **94 项中 93 通过**、`npx tsc -b` exit 0。

**过程中修正的一处既有测试期望**：`player-tuning-page.test.tsx` 原以精确相等断言保存载荷，B7-2 后载荷新增两个字段。改为 `objectContaining` 并补齐新字段，保留其"Anime4K 取值被写入"的原意——这是契约随功能增长，不是把失败改掉。

**仍未做（不冒充已完成）**：
- **真机播放验证**：没有启动桌面端、没有用真实外部整合包播放，因此"外部档实际能起播、整合包界面与脚本正常"只有单元与构建层证据。
- **B4 的视觉核对**（浅色/深色/窄窗口）与两条"来源卡是否改回方形 GridView"的既有失败断言，仍待确认口径。

---

# 13 底层优化批次（第二轮）

按用户 2026-09-15 指示：不动 UI 设计口径，专攻底层逻辑；允许联网检索；每批先补失败测试、最小实现、针对性验证，然后单独提交并推送。

**批次索引**（每批的完整说明在下方同名小节；提交为"代码 + 文档"成对提交）：

| 批次 | 主题 | 一句话结果 | 提交 |
|---|---|---|---|
| O1 | MPV `--script-opts` 覆盖语义 | Anime4K 默认模式与 thumbfast 缓存目录**从未生效**；改为追加式注入（真实 mpv 实测） | `c550835` |
| O2 | 续扫与取消 | 「继续扫描」100% 500（复用 scan_id 后重复登记）；暂停态无法取消 | `dbe3e6d` |
| O3 | 增量预算 / 分页 / 坏文件名 | 哈希变化不再膨胀请求、短页不再被当末页、非法条目名跳过计数 | `c987865` `0902d41` |
| O4 | 识别语义 | 混合产物失败不再被当成"只缺图片"；数字结尾片名不再被当年份 | `a9fd9d6` |
| O5 | schema v20 索引 + 读事务 | 三处全表扫描改为命中索引；投影不再因并发 rebuild 闪空 | `ffda2f8` |
| O6 | 产物重试增量 | 只补缺失图片、内容未变的 NFO 不重写（原先每次全量重下） | `d608b1e` |
| O7 | revision 级读取 | 2N 次单条 SELECT → 常量级批量查询 | `b9d7ac2` |
| O8 | 刮削 job 并发与心跳 | requeue 竞态不再双跑；下载期持续心跳；终态写入加守卫 | `2d344d2` |
| O9 | 失联恢复 | 读取完成后推进阶段，异常退出可离线续跑而非整库重扫 | `821a7ab` |
| O10 | 产物恢复语义 | 只缺图片时给出正确文案与动作，且不提供联网重试 | `211cfbc` |
| O11 | 死代码与检查债务 | 删除 9 处零引用符号；13 条 mypy + 9 条 ruff 告警清零 | `86f6c29` |
| O12 | 投影回退语义 | 一次瞬时刷新失败不再让已发布作品从媒体墙消失 | `ce2f39a` |
| O13 | 终态扫描断点清理 | failed/cancelled 清 frontier 与暂存检查点；paused 保留 | `1b36834` |
| O14 | TMDB 客户端复用 | 一次候选搜索 8~16 个客户端 → 1 个 | `b1e72e3` |
| O15 | 缓存热路径与读端点边界 | 命中不再重写整份索引、淘汰去掉 O(n²)；扫描详情默认不带证据 | `bd62a64` |
| O16 | 批量写入与来源卡取数 | `parsed_facts` 只在冲突候选上回读；扫描查询走 `root_id` 索引 | `5cc1e95` |
| O17 | 标题规范化两语义 | 身份与匹配各一份唯一实现，草稿评分与刮削排名不再结论相反 | `0df74c0` |
| O18 | 状态兜底与启动期写入 | 详情不再丢弃绑定状态兜底；启动补齐只写需要的行 | `a2a9f25` |

**总体验证状态**（第二轮全部完成后重跑）：后端仅剩 4 条既有失败（断言旧文案/CSS）；前端 `tsc -b` exit 0、`vitest` 196/197、契约 93/94；改动文件 `ruff` 与 `mypy` 全绿。**未做**：真机播放与桌面端视觉核对（由用户自行验收）。

**审查结论中"刻意不做"的条目（附理由/实测数据，不是遗漏）**：

| 条目 | 结论 | 依据 |
|---|---|---|
| A#8 进度累加在 250ms 窗口内失效 | 不做 | 证据批次路径本就**每条批次无条件**写进度，计数器只影响 handler 上报的节流；且两个上报源的 `processed_count` 语义不同，改动会让进度更不可信 |
| A#11 增量 state 大 JSON 每轮全量深拷贝/序列化 | 不做 | **实测**：10k 目录 deepcopy 22.6ms + dumps 9.2ms + loads 9.5ms + rolling 3.3ms ≈ 45ms（JSON 1.5MB）；30k 目录 ≈ 156ms。一次增量扫描本身要跑十几个远端请求（秒级），占比约 1%，不值得为此重构状态处理 |
| C#10 结构校验按 `(user_version, mtime)` 缓存 | 不做（只做了 `UPDATE jobs` 加 WHERE） | 严格结构校验是**安全网**：缓存判断失误会让损坏的 schema 被放行，代价是崩溃或数据错乱；启动期几毫秒不值得换 |
| C#12 识别证据接口默认分页 | 只做接口能力，不做默认截断 | 识别预览需要完整列表，截断会让预览不完整；已提供 `limit/offset/total`，前端分页属 UI 改动（当前冻结） |
| B#10 按审查建议"ranker 提为唯一实现" | 改为两语义并存 | 放宽身份语义会改写持久化身份键 → **重新导入已入库作品会生成重复作品**；收紧又会打挂既有契约（全角标点必须算同名） |
| 删除 `TMDBClient` 四个仅测试调用的方法、`AniListClient`、`_SUPPORTED_PROVIDERS` | 保留 | 有专门测试文件、配置里也有 `anilist_*` 字段，属**产品取舍**而非死代码，需产品决定 |

### O1 MPV `--script-opts` 覆盖语义导致注入失效（2026-09-15，已实现并实测验证）

**症状**：播放调校页把 Anime4K 模式设成 A/B/C 后行为没有变化；缩略图缓存也没有落到 `data/mpv-state/thumbfast`。

**根因（实测，不是推测）**：`--script-opts`（复数）是**覆盖**语义——同一命令行上出现多次时只有最后一次生效，前面的键被静默丢弃。KumiPlayer 连续注入了三条：

```
--script-opts=thumbfast.thumbnail=<state>/thumbfast
--script-opts=kumiplayer_anime4k.default_mode=<mode>
--script-opts=kumiplayer_anime4k.default_quality=<quality>
```

用自带干净 MPV v0.41.0 + 临时 Lua 探针读 `options/script-opts` 的**真实生效值**：

| | 生效值 |
|---|---|
| 修复前（生产参数原样喂给 mpv） | `[kumiplayer_anime4k.default_quality=balanced]` ← 前两键丢失 |
| 修复后 | `[thumbfast.thumbnail=…,kumiplayer_anime4k.default_mode=off,kumiplayer_anime4k.default_quality=balanced]` |

顺带实测排除同类风险：`--script` 是**追加**语义（两个探针脚本都加载），所以多处 `--script=` 的写法没有问题；`--list-options` 也明确写着 `--script-opt` 是 `--script-opts-append` 的别名。

| 文件 | 改动 |
|---|---|
| `backend/app/playback/mpv_runtime.py` | 三条 `--script-opts=` 改为追加式 `--script-opt=`，并把实测对照写进注释，避免以后被改回覆盖式 |
| `backend/tests/test_mpv_kumiplayer_integration.py` | 原断言更新为追加式；新增 `test_never_use_the_overwriting_script_opts_flag`：**禁止**出现覆盖式 `--script-opts=`，且同一个键不得注入两次 |
| `resources/mpv-runtime/README.md` | 加载机制示例改为逐条 `--script-opt=`，并加警示说明两种语义差异 |

**验证**：`test_mpv_kumiplayer_integration.py` / `test_mpv_external_mode.py` / `test_mpv_anime4k_menu.py` / `test_mpv_stats_zh.py` 全部通过；用生产代码生成的参数 + 真实 mpv 复测，三个键全部生效。`runtime-manifest.json` 的 `files`（27 项）不含 README 或配置，实测 `check_mpv_runtime(verify_files=True)` 仍是 `available / files_valid / configuration_available = True`，因此 `configuration_version` 无需变更（未改配置文件或 Lua 脚本）。

**为什么这是"底层"问题**：它不是显示错误，而是**用户设置从未到达播放器**——任何依赖 Anime4K 默认模式的观感调整都无效，且没有任何报错或日志提示。

### O2 修复「继续扫描」100% 失败与暂停态无法取消（2026-09-15，已实现并验证）

**来源**：第二轮底层审查（三个并行只读审查员 + 我逐条复核后才采用）。这是我在 B5-2b-2 自己引入的缺陷。

**根因（亲自读码确认）**：`register_source_scan` 把重复 scan_id 视为编程错误——`source_scan_runner.py:91-92` 直接 `raise ValueError("扫描任务已存在")`；而续扫分支在复用 `resume_scan_id` 之后**仍然走了一遍登记**（`api/media_v4.py:1523`）→ 必然 500，paused 行与 frontier 断点永久滞留。同时 `cancel` 端点只接受 `running/queued`（`api/media_v4.py:1571`），于是 paused 既不能续、也不能取消，来源卡永久停在「可继续扫描」。

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/source_scan_runner.py` | 新增 `resume_paused_scan()`：**原地**把 paused 行回置为 queued（清 `finished_at`/`error`、复位 `cancel_requested`、保留 `generation` 与 `started_at`——续扫是同一次巡检的继续，不是新代次） |
| 同上 | 新增 `cancel_paused_scan()`：paused 没有执行器在跑，直接落 `cancelled` 终态，不需要两段式取消 |
| `backend/app/api/media_v4.py` | 续扫分支改为调用 `resume_paused_scan()` 并**提前返回**（不再登记）；状态竞态返回 409 而不是 500 |
| 同上 | `cancel` 接受 paused：就地取消并 `scan_frontier.clear()` |

**验证**：新增 `backend/tests/test_v4_scan_resume_cancel.py` **3 条**（走真实 `TestClient` + 端点）：续扫复用同一 scan_id 且表内仍只有一行、`generation` 不变、frontier 保持 `completed=1 / pending=1`；非 paused 续扫返回 409；paused 取消落 `cancelled` 且断点行清零。全量 `backend/tests` 仅剩 4 条既有失败。

**诚实说明**：这个缺陷本该由 B5-2b-2 的测试抓到——当时我把 `resume_scan_id` 的 409 校验标注为「无自动化测试覆盖」就收工了，而真正会炸的是**成功路径**。教训：新入口至少要有一条走通成功路径的端到端测试，只覆盖错误分支不算覆盖。

### O3 待办（本轮审查产出、已逐条复核、按序实施）

三个只读审查员共报 36 条，我已复核完前几条，按「影响 × 触发概率」排定后续批次（每条都要先写失败测试）：

1. **增量核对的哈希扩散**：`incremental.py` 把"本目录清单哈希变化"传给全部直接子目录，`DEFAULT_VERIFICATION_BUDGET=12` 形同失效（一次增删可扩散成近全量重列）。
2. **页终止判据硬编码 100**：`scanner.py` / `incremental.py` 用 `len(entries) < 100` 判末页，`per_page` 被上游截短且 `total` 缺失时会静默丢条目。
3. **单个非法文件名 abort 整轮扫描**：`openlist/client.py` 的 `validate_entry_name` 抛错直接终止整个目录列举（函数注释却写"已跳过"）。
4. **混合失败被误判为"只缺图片"**：`jobs/completeness.py` 的 reason 子串兜底会把"缺 NFO"洗成 `artifact_state=degraded` 并放行进媒体墙，与既定"非图片产物失败仍算 failed"矛盾。
5. **数字结尾标题被当年份**：`Blade Runner 2049` → 标题 `Blade Runner` + 年份 2049，年份进入身份键后会被 ranker 硬阻断，永不自动刮削。
6. **索引与读事务**（需 schema v19→v20 迁移）：`revision_bindings(work_id)`、`artifacts(revision_id, work_id, artifact_type)`、`jobs(revision_id, ...)` 三个索引缺失导致投影重建/刮削 O(作品×绑定)；投影 `current()` 缺读事务，与 rebuild 的 DELETE 竞态会返回空 cards。
7. **来源卡为两个计数重建整份 draft 图**：`source_libraries.py` 每 1.5 秒为每个 draft 来源重建全量图（3 万条证据 ≈ 6 万条 SELECT），实际只用到 works/issues 两个数量。
8. **`retry_artifacts` 并非增量**：无视本地已有可用文件全量重下 N+3 张图并重写全部 NFO，与端点文档承诺的"只重新下载缺失的图片产物"不符。

### O3 增量核对请求膨胀与页终止判据（2026-09-15，已实现并验证）

**来源**：第二轮审查 A#4 / A#5，均已亲自复核代码后实施。

**① 清单哈希变化把全部子目录拉进同一轮（我在 B5-2c 引入的）**

子目录判定写成了 `changed = mtime 变化 or directory_content_changed`，而 `directory_content_changed` 的含义是"**本目录**的直接清单变了"——于是一个根目录级的增删会把所有一级子目录标记成变化并入队；`queued` 只增不减、`processed` 无上限，`DEFAULT_VERIFICATION_BUDGET = 12` 形同失效（6 个子目录时一轮请求从 2 变 8，300 个子目录就是 300+ 次请求，直接撞风控）。

修法：**没有**采用审查建议的"删掉哈希传播"，也**没有**采用我第一版的硬上限（它会打挂既有契约"变化优先是追加而不是替换滚动抽查"——`test_v4_openlist_incremental.py:252` 明确锁定 root + 滚动 + mtime 变化 = 3 次请求）。最终改为**优先级提示**：哈希变化时把该目录的子目录 `last_verified_at` 归零，让下一轮滚动抽样优先挑中它；本轮只核对"新增"或"mtime 变化"的子目录。同时修正 `changed_directories` 统计为"被判定变化/新增且本轮真正核对过"的数量（原实现会把已被滚动抽样选中的变化目录漏计，把"变化优先"误报成 0）。

**② 页终止判据硬编码 100**

`scanner.py` 用 `len(entries) < 100` 判末页，请求也用字面量 100。当驱动把每页截得比请求值短、或 `total` 缺失（浏览链路的既有测试已承认 `total` 可能缺失）时，短页被误判成末页 → 该目录后续条目被静默丢弃，而目录仍被标记 `completed`（续扫也不会补）。改为：**空页才是确定的末页**；有 `total` 时按 `page * per_page >= total` 封顶；请求与判据共用同一个 `per_page` 变量。

验证：新增 `backend/tests/test_v4_incremental_budget.py` **3 条**——① 6 个子目录 + 根目录新增一个：第二轮请求恰好 3 次（根 + 1 个滚动抽查 + 新增），未变化子目录一律不进本轮；② 哈希变化把子目录降级为下一轮优先核对（子树变化不会永久漏掉）；③ 服务端每页只给 2 条且不给 `total` → 必须翻到空页，5 条证据全部收集（旧实现只拿到 2 条）。既有增量契约测试**全部保持通过**——这正是"既有语义没有被破坏"的证据。全量 `backend/tests` 仅剩 4 条既有失败。

### O3-3 单个非法条目名不再拖垮整轮扫描（2026-09-15，已实现并验证）

**来源**：第二轮审查 A#6，已复核。`openlist/client.py` 的 `_list_dir_request` 在条目循环里直接调用 `validate_entry_name` 并让异常冒泡——但报错文案写的是"已跳过"。网盘侧完全合法的 `Show: Extra 01.mkv`（含 `:`）会让**整个目录**连同整轮扫描失败，用户只看到"扫描失败"。

**修法**：条目级 `try/except`——非法名跳过并计数，不再 abort；`OpenListDirPage` 新增 `skipped_entries` 字段；`scanner` 与 `incremental` 各自累加并写入 `scan_stats["skipped_entries"]`，让"被跳过的条目"可见，而不是静默消失。顺带修掉 `incremental._list_all()` 里与 `scanner` **完全相同**的"短页=末页"判据（O3-② 当时只修了 scanner，增量路径仍是旧判据，属于我自己漏掉的一半）。

**一处契约变更（有意为之）**：`test_openlist_client.py::test_malicious_entry_name_raises` 原断言 `list_dir` 对路径穿越条目抛 `OpenListValidationError`；现改为断言"被拒条目不出现在结果里，且其余正常条目照常返回"。**安全属性没有削弱**：条目路径只由**校验后的 name** 与父目录拼接（`join_remote_path`），服务端提供的 `path` 从不参与，被拒条目根本不进入该函数；而旧行为让一个坏名字毁掉同目录全部正常媒体，代价明显更大。`validate_entry_name` 本身仍保持硬报错。

验证：新增 `backend/tests/test_openlist_skipped_entries.py` **4 条**（非法名跳过并计数；`..` 与空名仍被拒且计数；正常名不受影响；`validate_entry_name` 自身仍硬报错）；`test_openlist_client.py` / `test_v4_source_scanner.py` / `test_v4_incremental_budget.py` 全绿；全量 `backend/tests` 仅剩 4 条既有失败。

**顺带修掉的 CI 隐患**：`ruff check` 发现 `client.py` 与 `test_openlist_client.py` 存在**既有的**导入排序问题（用 `git stash` 对比确认不是本次引入）。因为 CI 的 lint 步骤只检查**改动过的文件**，我改了 `client.py` 就会让 CI 变红，因此一并修正；现在 `ruff check` 对本次全部改动文件全绿。

### O4 识别语义两处确定性错误（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#1 / B#3 / B#12。**三条都在动手前用纯函数亲自复现过**（下面括号内是我跑出来的原始值，不是审查员的转述）。

**① 混合产物失败被当成"只缺图片"**（`is_artwork_reason`/`artwork_only_reasons`/`artifact_only_failure`，`jobs/completeness.py`）

`artifact_only_failure` 在结构化原因列表判定失败后，还会退回到对**拼接后的 `reason` 字符串**做子串匹配。实测：`completeness = ["海报下载或发布失败", "缺少或损坏 3 个剧集 NFO"]` 时 `artwork_only_reasons = False`，但 `artifact_only_failure = True`——因为拼接串里含"海报"。后果：缺 NFO 的作品被放行为 `metadata_state=ready` 进入媒体墙，与既定「非图片产物失败仍算 failed」直接矛盾。

修法：**存在 `completeness` 列表时只信它**；仅对没有该列表的历史记录才退回单条 `reason` 文本判断（`reason_code == artifact_incomplete` 的短路仍优先）。

**② 以数字结尾的片名被当年份**（`_extract_year` / `title_cleaner` 的年份剥离）

实测：`_extract_year('Blade Runner 2049') = 2049`、`_parse_work_title_and_year('Blade Runner 2049') = ('Blade Runner', 2049)`。连锁后果有三处：标题被删短、年份记错、而年份进入身份键后会被 `ranker._year_level` 当成"年份差 ≥ 2"直接阻断候选 → 作品永远停在人工确认。

修法：新增 `title_cleaner.is_plausible_year()`——**裸数字**（"空格 + 四位"这类无年份标记的写法）必须满足 `1900 ≤ y ≤ 当前年 + 2` 才当年份；`.2005` / `(2005)` 这类带明确标记的写法不受影响。`title_cleaner` 的第 4 步剥离与 `media._extract_year` 共用同一判据。

**③ 同一模式里第一个越界匹配终结了整个模式**

实测：`_extract_year('作品.2160p.2019') = None`、`_parse_work_title_and_year('作品.2160p.2019') = ('作品.2160p', None)`——第一个 `.2160` 越界后直接换下一个模式，真实的 `.2019` 被整段丢掉（连 `.2160p` 都留在标题里）。修法：抽出 `_first_plausible_year()`，每个模式用 `finditer` 取**第一个合理**匹配。

验证：新增 `backend/tests/test_v4_artifact_only_failure.py` **7 条**（混合失败 False、仅图片 True、仅 NFO False、`artifact_incomplete` 原因码仍优先、无列表的历史记录保留文本兜底、非 failed 状态一律 False）与 `backend/tests/test_v4_year_parsing.py` **7 条**（数字结尾片名保留数字与 `None` 年份；合理年份仍被解析并剥离；`作品.2160p.2019 → 2019`；`.`/`()` 年份剥离不受影响；`is_plausible_year` 六个边界值）。全量 `backend/tests` 仅剩 4 条既有失败；`ruff check` 与 `mypy` 对本次改动文件全绿。

### O5 schema v20：三处缺失索引 + 投影读事务（2026-09-15，已实现并验证）

> **破坏性变更**：数据库 schema 19 → 20。纯加法（三个索引），真实库下次启动自动迁移；不需要重置媒体库。

**来源**：第二轮审查 C#1/C#2/C#3/C#5，全部用 `EXPLAIN QUERY PLAN` **亲自复现**。

**修复前的真实执行计划**：

| 查询 | 修复前 | 后果 |
|---|---|---|
| `revision_bindings` 的 work 级子查询 | `SEARCH rb USING INDEX idx_v4_bindings_revision (revision_id=?)`，`work_id` 仅后置过滤 | 从 confirmed revision 驱动 → 每个作品都要遍历该 revision 的**全部**绑定行；300 作品 × 3 万绑定 × 多个子查询 ≈ 6×10⁷ 次行访问/次重建，而每次 confirm 与**每个刮削任务成功**都会置脏 |
| `jobs WHERE revision_id IN (...)` | `SCAN jobs` | 来源卡每 1.5 秒轮询一次全表扫 + 临时 B 树排序；jobs 只增不减 |
| `artifacts` 的逐作品完整性复查 | `SEARCH ... USING INDEX sqlite_autoindex_artifacts_2 (revision_id=? AND artifact_type=?)` | `work_id` 仅后置过滤 → 每个作品重扫该 revision 的全部剧集 NFO |

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/persistence/schema_v4.py` | `V4_SCHEMA_VERSION` 19→20；`create_v20_structures()`（`idx_v4_bindings_work` / `idx_v4_artifacts_work` / `idx_v4_jobs_revision`）+ `migrate_schema_v19_to_v20()` |
| `backend/app/media_v4/persistence/database.py` | `PRAGMA user_version` 字面量同步为 `20`；新增 **`if version == 19` 显式迁移分支**；空库创建路径补 `create_v20_structures`；`== CURRENT` 幂等链与内存期望结构同步 |
| `backend/app/media_v4/projection/library.py` | `current()` 拆出 `_read_current(conn)` 并用 `BEGIN … COMMIT` 包住三条 SELECT |

**修复后的计划**（同一脚本实测）：`SEARCH rb USING INDEX idx_v4_bindings_work (work_id=?)` / `SEARCH jobs USING INDEX idx_v4_jobs_revision (revision_id=?)` / `SEARCH artifacts USING INDEX idx_v4_artifacts_work (revision_id=? AND work_id=? AND artifact_type=?)`。

**投影读事务为什么必要**：并发 rebuild 的收尾是 `DELETE FROM library_generations WHERE generation_id != ?`，而 `library_cards` 是 `ON DELETE CASCADE`。没有读事务时三条 SELECT 各看一次最新提交，若 rebuild 在"读到 generation"与"读 cards"之间提交，cards 查询返回空集 → `/api/library` 的 `works` 直接来自 cards → **媒体墙在一切正常的情况下闪空**，而脏标记已被对方清掉，`ensure_current()` 也不会补重建。WAL 下读事务会稳定持有快照。

**又踩到同一个坑（值得记住）**：第一次改完 `user_version` 已是 20，但执行计划**没变**——因为空库创建路径只调 `create_schema_v4()` + `create_v19_structures()`，新索引根本没建（与 B5-2a 的 frontier 表完全同类）。已在空库路径补上并加注释，同时把这条教训写进 `AGENTS.md`。

验证：

- 新增 `backend/tests/test_v4_projection_index_usage.py` **4 条**：三条真实查询必须命中对应新索引（并断言 `jobs` 不再是 `SCAN`），以及空库必须建出这三个索引（防止再次漏掉创建路径）。
- `backend/tests/test_v4_schema_migration.py` 新增 v19→v20 用例：先断言 v19 上**没有**这三个索引（否则无法证明迁移确实新增了它们），`initialize()` 后断言版本 20、三索引就位、来源记录未被改写。
- `backend/tests/test_v4_library_projection.py` 新增并发快照用例：用一个转发连接的代理在"读 cards 之前"提交一次**真实** rebuild，断言 `current()` 仍返回非空 cards；**并带反证**——同样竞态下直接调用 `_read_current()`（不包读事务）返回空 cards，证明该用例确实在检验这个修复，而不是靠运气通过。
- 全量 `backend/tests` 仅剩 4 条既有失败；`ruff check` 与 `mypy` 对改动文件全绿。

### O6 产物重试改为真正的增量（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#4（另有 C#4 的"来源卡草稿图重建"留到 O7）。已复核代码。

**问题**：`retry_artifacts` 的公开承诺是"只重新下载**缺失**的图片产物"，文档也这么写；但 `_materialize_local_artwork` 对每张剧照与 poster/fanart/clearlogo **无条件重新下载**，`publish_metadata_artifacts` 也无条件重写 Work NFO 与每集 NFO。24 集作品点一次"重新下载媒体图片" ≈ **27 个 HTTPS 请求 + 25 次写盘**——而这些下载正好落在"下载阶段没有心跳"的窗口里（新发现 B#5：超过 120s 会被失联回收误判为失败）。

**修法**：发布前先取该 `(revision_id, work_id)` 的**已发布产物集合**（`{（类型, 目标路径）: digest}`）：

| 位置 | 改动 |
|---|---|
| `jobs/metadata_artifacts.py` | 新增 `_published_artifacts()` 与 `_is_current()`（已发布 **且**磁盘文件存在非空才算可用）；`_materialize_local_artwork()` 增加 `published` 参数：已有可用产物时直接登记既有 digest 并跳过下载（剧照与三张作品图都覆盖） |
| 同上 | 新增 `_artifact_digest()`：NFO 内容与磁盘一致时不重写文件（写入本来是原子的，但没必要动盘、也不必刷新 `updated_at`），内容不同照常写 |

**验证**：新增 `backend/tests/test_v4_retry_artifacts_incremental.py` **3 条**——① 剧集作品首轮 4 次下载，第二轮**零下载**且 digest 复用既有值；② 只删掉海报文件后，第二轮**只重下海报**（URL 断言）；③ 走完整 `publish_metadata_artifacts` 路径重复发布：**零下载 + 零 NFO 重写**。全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

**未做（下一批）**：来源卡为 `len(draft_graph.works)` 与 `len(draft_graph.issues)` 两个计数重建整份草稿图（3 万条证据 ≈ 6 万条 PK 查询 + 18 万次 `json.loads`，而来源卡在有活动任务时每 1.5 秒刷新一次）。这条需要先确认 draft 阶段 `revision_bindings` 里到底有什么，才能用等价的 COUNT 替换而不改变卡片上"N 部作品"的数字——不能拿一个近似值顶上去。

### O7 revision 级读取改为批量（2026-09-15，已实现并验证）

**来源**：第二轮审查 C#4。**复核时我否掉了审查建议的修法**，理由如下。

审查建议是"把草稿图换成两条 COUNT（`revision_issues WHERE resolved=0` + `revision_bindings` 的 DISTINCT work_id）"。但读码发现来源卡是**故意**做实时评估的：`load_draft_graph(refresh_snapshot=False)` 的 docstring 明确写着"识别规则升级后，若只在内存中重算，导入页虽然已无问题，媒体管理来源卡仍会读取旧表而持续显示红色冲突"——也就是说，换成读表会**把"规则升级后旧的红色冲突又回来"这个已修的问题重新引入**。所以真正该优化的是**评估本身的成本**，而不是绕开评估。

成本集中在 `_load_revision_entries`：它逐条调用 `get_source_evidence` + `get_parsed_facts`，一个 revision 就是 **2N 次单条 SELECT**（3 万条证据 = 6 万次）。

| 文件 | 改动 |
|---|---|
| `persistence/repositories.py` | 新增 `get_source_evidence_bulk()` / `get_parsed_facts_bulk()`：按 `_BULK_BATCH_SIZE = 400`（与既有 `save_*_bulk` 一致）用 `IN (...)` 批量读取，返回对象与单条读取完全一致，缺失 id 同样抛 `KeyError` |
| `revisions/service.py` | `_load_revision_entries()` 改用批量读取；覆盖（overrides）与 tuple 归一化逻辑不变 |

验证：新增 `backend/tests/test_v4_revision_entries_bulk.py` **4 条**——① **仓储层等价性**：批量读取的 `{id: 对象}` 与逐条读取逐一相等（避免我在测试里重复实现覆盖逻辑，直接用仓储接口对比）；② 覆盖仍然生效且 `title_candidates` 被归一化为 tuple；③ **语句数不随条目数增长**（6 条与 60 条读取的 SELECT 次数完全相等，且 ≤ 8；用 `set_trace_callback` 统计）；④ 草稿图的 works 数量仍为 24/24（批量读取没有丢条目）。全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

**过程中的一次自我纠错**：等价性用例第一版把"逐条路径"写成不应用 overrides 的原始读取，于是断言失败。核实后确认那是我测试写错（生产路径本来就要应用覆盖），于是把等价性下移到**仓储层**（纯函数式对比，不重复生产逻辑），另立一条"覆盖生效"的断言——而不是把失败的断言改松。

### O8 刮削 job 的并发守卫与下载期心跳（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#5 / B#6（A#3 的恢复分支 stage 门控留到 O9）。均已复核代码。

**① `requeue_work` 是读后写**：先 `SELECT` 判 `status == 'running'`，再 `UPDATE ... WHERE job_id = ?`。并发两次重试时，后到的那次会在执行器 `claim_running` 之后把 `running` 改回 `queued`，runner 随即再次领取 → **同一个 work 被两个执行器同时跑**（重复 TMDB 请求与图片下载、交替覆盖 `scrape_bindings` 与 `artifacts`）。修法：守卫写进 UPDATE 本身（`AND status != 'running'`），`rowcount != 1` 时抛原有语义的错误。

**② 图片下载期没有心跳，且两个终态写入都没有守卫**：24 集作品 ≈ 27 张图，下载可能持续数分钟，而心跳停在 provider 阶段那一刻 → 超过 120s 被判"失联回收"为 `failed`；随后收尾的 `UPDATE jobs SET status='succeeded'`（失败分支同理）**没有 running 守卫**，会把终态又改回去——界面与实际执行结果自相矛盾，还掩盖了回收判断本身。修法：

- 两个终态写入加 `AND status = 'running'`；成功分支若 `rowcount != 1` 只记 warning、不打断 worker（产物与绑定已经写下，没必要回滚）；
- 把 `heartbeat` 通过 `on_progress` 回调一路传到 `_materialize_local_artwork`，**每张图下载前上报一次**。

验证：新增 `backend/tests/test_v4_scrape_job_state_guards.py` **4 条**——① 用转发连接的代理在 `UPDATE ... queued` 执行前让另一连接把任务置为 `running`，断言 `requeue_work` 拒绝且 `running` 未被覆盖（**竞态是确定性复现的，不靠时序碰运气**）；② provider 阶段把任务置为 `failed`（模拟失联回收），断言 process 收尾后仍是 `failed` 并保留原 `last_error`；③ 集成用例：下载阶段结束后 `heartbeat_at` 必须晚于下载前；④ `publish_metadata_artifacts(on_progress=...)` 每张图上报一次（3 张图 = 3 次）。全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

**加守卫前先确认的一件事**：`process()` 自己会调用 `claim_running()`（因此执行期状态恒为 `running`），所以加 `status='running'` 守卫**不会**让 `retry_artifacts` 这类直接调用 `process()` 的路径卡在 `queued`——这是读码确认过的，不是想当然。

**仍未做**：A#3 异常退出后的恢复分支被 stage 门控挡死（`stage` 在执行期一直是 `reading_source`，而分支 B 要求 `parsing/normalizing/preparing_preview/recognizing`）→ local/OpenList 异常退出只能整库重扫。修法需要在"证据落库之后、handler 返回之前"推进 stage，属状态机改动，留下批单独做。

### O9 读取完成必须推进扫描阶段（2026-09-15，已实现并验证）

**来源**：第二轮审查 A#3。复核代码时发现**审查建议的修法（"让分支 B 接受 `reading_source` + 证据完整"）会造成更严重的问题**，因此采用了另一种修法。

**问题**：`recover_stale_scans()` 的"离线续跑"分支要求 `stage ∈ {parsing, normalizing, preparing_preview, recognizing}`，而 handler 执行期**没有任何代码推进阶段**（`durable_scan` 在读取期只写 `reading_source`）。于是"最后一批证据已落库、finalizer 尚未开始"这一刻进程退出 → 被判 `failed`「上次扫描意外中断，请重新扫描」→ local/OpenList 只能整库重扫。既有测试 `test_v4_source_scan_recovery.py` 是**手工把 stage 种成 `parsing`** 才通过的，正好绕过了这个门控。

**为什么不能只删掉阶段条件**：`durable_scan._update_scan_progress` 每次上报都会把 `total_count` 拉到 `processed_count`（`next_total = max(current_total, total_count, next_processed)`），所以**读取进行到一半时两个数字同样相等**。若分支 B 只看计数，读到一半崩溃的扫描会被误判成"证据完整"，进而用**残缺证据**建立 draft——那比要求重新扫描严重得多。阶段条件正是防这件事的。

**修法**：在读取真正结束的那一刻（`_settle_total` 之后、finalizer 之前）把阶段推进到 `parsing`，让 stage 如实反映"读取已完成"。这样被杀进程留下的状态是 `running + parsing`，分支 B 自然命中；而读到一半崩溃仍然停在 `reading_source`，继续走"要求重新扫描"。

验证：新增 `backend/tests/test_v4_scan_stage_advance.py` **3 条**——① 在 finalizer 内读取 `source_scans.stage`，断言此时已是 `parsing`（用真实 runner + 假 handler 驱动，finalizer 故意抛错）；② 把该行改回"失联的 running + stale 心跳"后 `recover_stale_scans()` 必须**离线续跑完成**（`completed` + draft 建立），而不是要求重新扫描；③ **反证**：读到一半崩溃（`stage='reading_source'`、计数相等）必须仍判 `failed` 且**不得建立 draft**，证明阶段条件没有被削弱。`test_v4_source_scan_recovery.py` 全套保持通过；全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

**过程中的一次认知纠正**：第一版用例断言"finalizer 崩溃后 stage 应为 parsing"，实测是 `failed`——因为被捕获的异常会走 `_finish_scan`，它把 `stage` 一并写成终态。核实后确认**进程被杀（无收口）才保留 `parsing`**，于是改成在 finalizer 执行时捕获阶段，而不是把断言改松。

### O10 只缺本地图片时的恢复动作与文案（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#8。

**问题**：`degraded` 分支（仅图片产物缺失）**不写 `reason_code`**，于是恢复策略只能靠文本兜底 → `action="none"`，文案落到兜底句「媒体信息需要处理，请检查后重试」——**指向在线资料**，而真正该做的是重下图片；B1-2 早已写好的 `artifact_incomplete` 友好文案（`service.py:79-80`）**永远走不到**。更危险的是：一旦真的带上该原因码，原策略的兜底会把它映射成 `retry_metadata` → 发起**一轮完整联网抓取**，与「只重新下载缺失图片」的零网络承诺直接冲突。

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/jobs/scrape.py` | 仅图片缺失的降级分支写入显式 `reason_code="artifact_incomplete"`（资料状态保持 `ready`、产物 `degraded`，作品照常进媒体库） |
| `backend/app/media_v4/revisions/service.py` | `metadata_recovery_policy` 新增产物上下文分支：`artifact_incomplete` 且（`artifact_state=degraded` 或完整性原因全是图片）→ `action="none"` + 提示「可只重新下载缺失的媒体图片，不需要重新获取在线资料」 |
| 同上 | `_metadata_recovery_action_fallback` **保持** `artifact_incomplete → retry_metadata`（见下） |

**为什么不是一刀切**：既有测试 `test_v4_work_execution_detail.py` 锁定「`artifact_incomplete` 且**没有**产物上下文 → `retry_metadata`」。这类历史快照既没有 `degraded` 标记、也没有图片补全入口，若一律返回 `none`，用户将**完全没有可用动作**。因此按产物上下文区分：有新上下文 → 图片重下路径（`none` + 明确提示），旧快照 → 保留原来的可用动作。第一版我就是一刀切，被这条既有契约测试挡了下来。

验证：新增 `backend/tests/test_v4_artifact_recovery_policy.py` **3 条**——① 产物上下文下 `action="none"`、文案含「图片下载或发布失败」且不含「需要处理」、提示指向重下图片；② 有/无产物上下文两条路径分别得到 `none` 与 `retry_metadata`；③ **走真实刮削路径**（假 `assess_metadata_completeness` 返回纯图片原因）：断言落库的 `metadata_json` 同时具备 `metadata_state=ready`、`artifact_state=degraded`、`reason_code=artifact_incomplete`，且对应策略为 `none`。全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

### O11 死代码清理 + 静态检查债务清零（2026-09-15，已实现并验证）

**来源**：第二轮审查 C#6/C#12 与死代码清单。**每个候选都做了区分大小写的精确引用核查**——PowerShell 的 `Select-String` 默认不区分大小写，第一轮计数把 `_STAGE_ORDER`、TS 里的 `stageLabel` 也算了进来，差点误判。核查后确认 11 处中的 9 处为零引用。

**删除（零引用）**：

| 位置 | 内容 |
|---|---|
| `sources/scan_state.py` | `ACTIVE_SCAN_STATUSES`、`TERMINAL_SCAN_STATUSES`、`STAGE_LABELS`、`stage_order()` 及其 `_STAGE_ORDER`（阶段表与 `durable_scan.py` 那份重复且无人使用） |
| `integrations/openlist/cache.py` | `_path_key()`（`_page_key` 的重复实现，注释自称"兼容保留"） |
| `sources/durable_scan.py` | `_scan_root()`、`scans_data_root()`（后者还让 `get_data_dir` 变成未用导入） |
| `sources/source_scan_runner.py` | `_ExecutionRuntime.request_cancel()` |
| `resolution/resolver.py` | `_relation_work_key(facts)`（实际使用的是 `_relation_work_key_from_row`） |
| `sources/scanner.py` | `METADATA_SUFFIXES` |
| `media_v4/__init__.py` | 陈旧的 `V4_SCHEMA_VERSION = 4` 副本 |

**保留（刻意不删，附理由）**：`TMDBClient` 的四个方法、`AniListClient`、`_SUPPORTED_PROVIDERS`、`directory_counts`。前两者有专门测试文件、配置里也有 `anilist_*` 字段，属**产品取舍**而非死代码；`directory_counts` 被 15 处测试引用，是合法 API。删这些需要产品决定，不擅自删。

**顺带清掉的静态检查债务**（CI 只对**改动文件**跑 ruff/mypy，所以我碰过的文件必须干净）：

- **13 条 mypy**：`durable_scan.py` 两处空字典被推断成 `Callable[[], bool]` 后写入函数与字符串（既有问题）；`scanner.py` 的 frontier 回调 None 收窄（**我自己 B5-2b-1 引入**——第一版用 `if ...: assert ...` 无效，assert 的收窄只在块内有效，改为在每处调用点带上 `is not None`）；`scanner.py` 两处 `evidence` 注解；`resolver.py` 的 `number_frequency` 注解与 `allocation_key` 两种元组形状混用（重命名为 `title_allocation_key`）。
- **9 条 ruff E402**：`resolver.py` 把 `NON_BLOCKING_ISSUE_CODES` 定义在 import 之前（**我自己 B3 引入**），已移到 import 之后。

**新增守卫**：`backend/tests/test_v4_schema_version_source.py` **4 条**——① `database.py` 的 `PRAGMA user_version = N` 字面量必须与 `V4_SCHEMA_VERSION` 一致且**只允许一处**；② 版本号只允许定义在 `schema_v4.py`；③ `app.media_v4` 不得再暴露版本号副本；④ 已删除的 `scan_state` 常量不得被重新引入。这类"常量与字面量漂移"已在 v19/v20 咬过两次，注释里的承诺不如一条测试。

验证：全量 `backend/tests` 仅剩 4 条既有失败；改动文件 `ruff` 与 `mypy` **全绿**（含修掉的 13 + 9 条历史告警）。

### O12 已发布作品不再因一次瞬时刷新失败而从媒体墙消失（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#2。

**问题**：投影为每个作品只取**最新一条** `scrape_bindings`，而 `/api/library` 默认只放行 `metadata_state == "ready"`。已发布作品在往目录里加一集后触发的新 revision 抓取若瞬时失败（限流、超时、服务端 5xx），最新行就是失败 → **整条作品从媒体墙消失，标题与海报一起丢**。这与 `_retain_successful_details` / `_artifact_view` 的既有意图相反。

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/projection/library.py` | 取数 CTE 拆成 `scrape_rows`（同时算 `latest_rank` 与 `confirmed_rank`）、`latest_scrapes`（最新一行）、`latest_confirmed`（同一作品最近一次 confirmed 结果），并 LEFT JOIN 出 `previous_ready_revision_id` / `previous_ready_metadata_json` |
| 同上 | 新增 `_recover_previous_ready()`：最新行是失败/等待态时，若同一作品上一次成功结果仍为 `ready`**且**投影复查确认产物完整，则保持 `ready` 并沿用上次的标题/海报，同时写入 `metadata_refresh_error` 标注"当前显示的是上一次成功结果"；上次结果不是 ready 或产物已损坏时**不回退**，避免掩盖真正坏掉的作品 |

验证：`backend/tests/test_v4_library_projection.py` 新增 **3 条**——① 失败行更新时卡片仍 `ready`、标题来自上次成功快照、带 `metadata_refresh_error`，且产物复查针对**上一次成功的 revision**；② **反证**：上次结果产物损坏（缺 NFO）时不回退，卡片仍是 `failed` 且无 refresh_error；③ 反向顺序（失败行更旧）不触发回退，既有语义不变。既有投影测试（含"取最新 scrape 元数据"）全部保持通过；全量 `backend/tests` 仅剩 4 条既有失败；`ruff`/`mypy` 全绿。

**一处测试事故（记录在案，供后人避坑）**：我用 `edit` 替换锚点时，把某个既有测试的 `def` 行一并替换掉了，于是那个测试体变成函数内死代码——**文件仍能通过 `compileall`**，静态检查发现不了，测试是**静默消失**的。靠 `Select-String '^def test_'` 列出函数清单才发现并修复。教训：把"一行代码"当锚点前，先确认它不是结构声明（`def`/`class`/装饰器）。

### O13 终态扫描的断点清理（2026-09-15，已实现并验证）

**来源**：第二轮审查 A#7。复核后确认两类"进行中的断点"只增不减：

- `source_scan_directories`（目录级 frontier）按**目录数**增长，成功路径才清（`scan_handlers`），取消暂停态在 O2 补清；
- `openlist_incremental/staged/<scan_id>.json` **每个 scan 一个文件**，只有确认成功时被 `activate_scan_state` 消费并删除。失败/取消/放弃草稿的扫描会永久留下它。

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/sources/incremental.py` | 新增 `discard_scan_state(scan_id)`：删除该 scan 的暂存检查点（幂等） |
| `backend/app/media_v4/sources/source_scan_runner.py` | 新增 `_discard_scan_checkpoints()`，并在 `_finish_scan()` 收口为 **failed / cancelled** 时调用（清 frontier + staged） |

**为什么只清终态**：`paused` 必须保留断点，否则「继续扫描」失去意义（O2 的语义）。清理也不会污染后续增量——发布基线用的是 `active/<root_id>.json`，缺失检查点只会让下一轮把目录当成"未核实过"多核对几个，不会用错基线。

验证：新增 `backend/tests/test_v4_scan_checkpoint_cleanup.py` **5 条**——① failed 清空 frontier 与 staged；② cancelled 同样清空；③ **反证**：paused 必须完整保留（frontier 2 行 + staged 1 个文件）；④ 没有断点时清理幂等且终态照常写入；⑤ 清理目标是可解析的暂存 JSON（避免误删别的东西）。全量 `backend/tests` 仅剩 4 条既有失败。

**顺带清掉的静态检查债务**：`incremental.py` 有 **15 条既有 mypy 错误**（`directories` 的值类型写成 `float | None` 却含字符串字段；同一函数里 `item` 先绑定 `SourceEvidence` 再绑定 `OpenListEntry`）。因为 CI 只对**改动文件**跑 mypy，我碰了这个文件就必须清干净：修正标注为 `dict[str, object]`、把基线循环变量改名为 `baseline_item`。现在改动文件 `ruff`/`mypy` 全绿。

### O14 候选搜索复用同一个 TMDB 客户端（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#7。

**问题**：`default_candidate_search` 对最多 8 个 query 逐个调用 `search_tmdb_candidates`，而后者每次都新建 `TMDBClient` → 每个 query 重新握手 TCP/TLS，`_response_cache`、连接池与限速状态全部作废；随后的别名富化也只为**每个候选**再新建客户端。叠加全局串行限流后，一次候选搜索的网络成本被放大成 N 份。

**修法**：

| 文件 | 改动 |
|---|---|
| `backend/app/media_v4/jobs/metadata.py` | `search_tmdb_candidates(..., *, client=None)`：传入则复用，不传保持原行为；新增 `open_tmdb_client()` 统一按配置构造客户端（无 Token 返回 None），调用方不必知道怎么建 |
| `backend/app/media_v4/resolution/candidates.py` | `default_candidate_search` 用**一个**客户端跑完全部 query 与别名富化 |

验证：新增 `backend/tests/test_v4_tmdb_client_reuse.py` **4 条**——① 5 个 query 只创建 1 个客户端；② 不传 client 时仍自己开一个（默认路径不变）；③ 注入 client 时不再新建；④ 未配置 Token 时零客户端。全量 `backend/tests` 仅剩 4 条既有失败。

**过程中的两个真实踩坑（都值得记住）**：

1. 我最初在 `candidates.py` 里直接 `from app.scrape.tmdb_client import TMDBClient` 并新建客户端，结果**打挂了既有 P-001 测试**——那条测试打桩的是 `metadata.TMDBClient`，我在别处新建就绕过打桩、打到真实网络。改成由 `metadata.open_tmdb_client()` 统一构造后，既有测试**无需修改**即通过，分层也更合理（candidates 不该知道怎么建 TMDB 客户端）。
2. 我自己写的第一版测试只 patch 了 `app.scrape.tmdb_client.TMDBClient`，而 `metadata.py` 在**模块级**绑定了这个名字 → 用例真的发起了网络请求（日志里看到 TMDB 超时）。测试必须同时替换目标模块里已绑定的名字。

**顺带清掉的静态检查债务**：`metadata.py` + `candidates.py` 有 **27 条既有 mypy 错误**——`plan_work_candidates` 里 `identity` / `existing` 在三段循环中被复用成不同实体（Work / Episode / Asset / Relation），mypy 按首次绑定定型后全线冲突；另有两处 `int | None` 写入 `int`。已按实体改名（`existing_work` / `existing_episode` / `existing_asset`、`episode_identity` / `asset_identity` / `relation_identity`）并补 `relations_by_identity` 标注。这些**不是运行时 bug**，但改名后该函数的意图清楚得多；现在改动文件 `ruff` + `mypy` 全绿。

### O15 浏览缓存热路径与读端点边界（2026-09-15，已实现并验证）

**来源**：第二轮审查 A#10 与 C#12。

**① 浏览缓存每次命中都重写整份索引（A#10）**：`read_cache` 命中时要维护 LRU 的 `last_accessed_at`，但每次命中都 `_read_index` + 改写 + `_write_index`（`MAX_CACHE_PATHS = 2000` 时约数百 KB），而浏览翻页正是高频操作。同时 `_evict_locked` 的淘汰循环**每轮都 `sum()` 全量重算**总字节，最坏 O(n²)。

修法：新增 `INDEX_WRITE_MIN_INTERVAL_SECONDS = 60`，命中只在"距上次落盘的访问时间超过该间隔"时才写回（LRU 只需要分钟级精度）；索引项缺失时仍立即补登记并落盘（否则该条目永远不进 LRU）。淘汰改为**一次性算总量 + 删除时按条目 size 递减**，行为等价但复杂度从 O(n²) 降到 O(n log n)。

验证：新增 `backend/tests/test_openlist_cache_index.py` **5 条**——① 窗口内 5 次命中零写回且照常返回内容；② 超过间隔后必须落盘一次（LRU 不失真）；③ 索引项缺失时命中会补登记（含 `size_bytes` 与访问时间）；④ 路径数上限下按 `last_accessed_at` 淘汰最旧、保留最近 3 条；⑤ 字节上限极小时全部淘汰。

**② 读端点默认值"顺手"就能拉全量（C#12）**：`/sources/scans/{scan_id}` 原先 `include_entries=True`，任何漏传参数的调用方都会拿到数万条证据（数十 MB JSON）。

修法：默认改为 `False`，只有显式 `include_entries=true` 才带条目（前端一直显式传参，唯一依赖默认值的测试已改为显式请求）。`/imports/{revision_id}/evidence` 默认仍返回**全量**——识别预览需要完整列表，截断会让预览不完整；但新增 `limit` / `offset` 与 `total` / `returned` / `offset` 字段，让程序化调用方能自我约束。

验证：新增 `backend/tests/test_v4_read_endpoint_bounds.py` **3 条**——① 默认不带条目但 `evidence_count` 如实报告；② 显式请求时返回条目；③ 证据接口默认全量且 `total` 始终为完整条数，`limit/offset` 返回的正是对应切片。全量 `backend/tests` 仅剩 4 条既有失败；改动文件 `ruff` + `mypy` 全绿。

**刻意没做**：
- **A#8（进度累加的 250ms 窗口失效）**：复核后发现影响远小于审查描述——证据批次路径（`persist_evidence_batch`）本就**无条件**每条批次写进度，`_items_since_write` 只影响 handler 上报的进度节流，而且两个上报源的 `processed_count` 语义不同（库内行数 vs 内存证据数），贸然改会让进度更不可信。因此不做，记录在案。
- **识别预览的前端分页**：那属于 UI 改动（当前明确冻结），接口已具备分页能力。

### O16 批量事实写入与来源卡取数（2026-09-15，已实现并验证）

**来源**：第二轮审查 C#11 与 C#9。

**① `save_parsed_facts_bulk` 的不可变校验会全量回读（C#11）**：插入后把**全部** facts 按 id 回读、逐行重建对象（`_row_to_parsed_facts` 每行 6 次 `json.loads`）再与入参比较。但 `INSERT OR IGNORE` 刚插入的新行必然等于入参——回读它们纯属浪费；3 万条 facts 的导入期等于 3 万行回读 + 18 万次 `json.loads`。

修法：先只取一次**已存在的 id 列表**（SELECT 单列、不解析 JSON），插入后仅回读这些冲突候选再比较。全新导入因此零回读，幂等重放只回读真正已存在的行。

**② 来源卡全表扫 `source_scans` + 逐 root 新开连接 COUNT（C#9）**：`source_scans` 每次扫描 +1 行且从不清理，来源卡却整表取回再在 Python 端分组；另外对"没有 revision"的 root 会另开连接做 `COUNT(*)`。

修法：扫描查询改为**只针对本次要出卡的活跃 root** 过滤，并用窗口函数选每个 root 的最新一行——`EXPLAIN` 实测由全表扫描变为 `SEARCH ss USING INDEX sqlite_autoindex_source_scans_2 (root_id=?)`（走 `UNIQUE(root_id, generation)` 索引）。

**两处我做错又改回来的改动（都值得记录）**：
1. 我最初顺手把"没有 revision 证据时"的数字改成优先取扫描行的 `total_count/processed_count`，想省掉那次 COUNT——既有测试立刻指出问题：**扫描行存的是进度值**（读取期由内存计数推进，可能领先于真实落库行数），来源卡会显示比实际更多的文件。已撤回、保留权威 COUNT，并在注释里写明原因。
2. 过滤集合我也用错过一次：先按"有 revision 的 root"过滤，打挂了"有扫描但还没 revision"的来源卡（4 条既有测试），改为按本次遍历的 `roots` 过滤。

验证：新增 `backend/tests/test_v4_persistence_and_card_efficiency.py` **6 条**——① 全新导入 300 条零回读；② 幂等重放只回读已存在的 50 条；③ 同 id 内容变化仍抛"不可变冲突"；④ revision 有证据时不为一个数字再做 COUNT；⑤ 无证据时以权威 COUNT 为准（且恰好查一次）；⑥ 同一 root 多次扫描只取最新一次。全量 `backend/tests` 仅剩 4 条既有失败；改动文件 `ruff`/`mypy` 全绿。

### O17 标题规范化：两个语义、各一份实现（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#10。**复核时否掉了审查建议的修法**（见下）。

**问题**：项目里有四份同名私有 `_normalize_title`，三份实现完全相同（`candidates` / `resolver` / `service`：NFKC + casefold + 空白收敛 + 去两端装饰标点），一份不同（`ranker`：只保留字母数字、连标点与空格一起丢）。同一对标题因此在**草稿候选评分**里判不等、在**刮削排名**里判等值——用户看到"导入预览说没把握、刮削却自动采用了"（或反向）。

**为什么没有按审查建议"提升 ranker 为唯一实现"**：那要求 `resolver._normalize_title` 也改用宽松语义，而它参与**持久化身份键**（`title:<标题>:<年份>:<类型>`）——放宽后同一个标题会算出不同身份键，**重新导入已入库的作品会生成重复作品**。逆向（把 ranker 改成严格语义）同样不行：`test_v4_candidate_ranker.py::test_cjk_punctuation_difference_still_counts_as_the_same_title` 明确锁定"中文全角标点差异必须仍算同名"，改成严格会让唯一正确候选降级、退回人工确认。

**修法**：承认这里其实是**两个不同的问题**，各留一份唯一实现，放进新模块 `resolution/title_norm.py`：

| 函数 | 回答的问题 | 语义 | 使用者 |
|---|---|---|---|
| `normalize_identity_title` | 这是**哪个作品** | NFKC + casefold + 空白收敛 + 去两端装饰标点（正文标点保留） | `resolver`（身份键）、`candidates`、`service` |
| `normalize_match_title` | 这是**不是同一个标题** | 同上再丢掉全部标点与空格 | `ranker`、`candidates._score_candidate` |

四份私有函数全部改为委托共享实现（行为逐一对齐，`ranker` 与另外三份保持各自的既有语义），并让 `candidates._score_candidate` 改用**匹配**语义——它与刮削排名回答的是同一个问题。

验证：新增 `backend/tests/test_v4_title_normalization_unity.py` **6 条**——① 身份语义保留正文标点、剥掉两端装饰标点（NFKC 会把全角 `！` 折成半角）；② 匹配语义忽略全部标点与空格（`Re:Zero` = `Re Zero`）；③ **四份私有函数逐一委托共享实现**（对 8 个样例逐一比对）；④ **核心跨组件一致性**：同一对只差标点的标题，`ranker._title_identity_level` 判 2（完整等值）且 `_score_candidate` 给 `high`；⑤⑥ 反证：身份语义**没有**被放宽（标点差异仍算出不同身份键 → 不会产生重复作品）。全量 `backend/tests` 仅剩 4 条既有失败；改动文件 `ruff`/`mypy` 全绿。

### O18 执行详情状态兜底与启动期无谓写（2026-09-15，已实现并验证）

**来源**：第二轮审查 B#9 与 C#10。

**① 执行详情把绑定行状态兜底覆盖成空（B#9）**：详情投影先算 `metadata_state = str(metadata.get("metadata_state") or metadata_state)`，紧接着 `metadata_state, artifact_state, artifact_reasons = _artifact_view(metadata)`，而 `_artifact_view` 返回的是 `payload.get("metadata_state") or ""` —— 兜底立刻被空值覆盖。于是历史快照（`metadata_json` 里没有 `metadata_state`）会出现**进度列表显示 `confirmed`、执行详情显示空状态**。

修法：`_artifact_view(metadata, *, fallback_state="")` 接收兜底值，详情路径传入绑定行状态，且在其返回为空时保留原值；`get_execution_progress` 侧本来就只在非空时覆盖，现在两个投影共用同一函数、同一套兜底规则。

**② 每次启动都对整张 `jobs` 表做一次覆盖写（C#10）**：`migrate_schema_v15_to_v16` 在幂等链里每次启动都会执行，而它的 `UPDATE jobs ... CASE` 原本**没有 WHERE**；jobs 只增不减（每作品每 revision 至少两条），WAL 放量随导入次数线性增长。

修法：WHERE 写成语义上的反条件（`heartbeat_at = ''`、或 running 且 `started_at = ''`、或终态且 `finished_at = ''`）——只有真正需要补齐的行会被写，行为完全等价。

**刻意没做**：审查还建议把"内存期望库结构校验"按 `(user_version, 文件 mtime)` 缓存进 `v4_meta`。那是**严格结构校验这道安全网**：一旦缓存判断失误，损坏的 schema 会被放行，代价是应用崩溃或数据错乱。启动期几毫秒的校验不值得拿它换，记录理由后不做。

验证：
- `backend/tests/test_v4_work_execution_detail.py` 新增 **2 条**：① 历史快照缺 `metadata_state` 时详情必须退回绑定行状态（`confirmed`）；② 有明确状态时仍以 `metadata_json` 为准（`ready`）。
- `backend/tests/test_v4_schema_migration.py` 新增 **1 条**：5 条 job（1 条需补齐、4 条完整）跑迁移后 `conn.total_changes` 只增加 **1**（旧实现会改动全部 5 条）。
- 顺带修掉我自己 O7 测试里的**测试污染**：那条用例在循环里调用 `monkeypatch.undo()`，会把 autouse 夹具（数据目录隔离）的补丁一并撤销，导致"单独跑通过、全量跑失败"。改用 `monkeypatch.context()` 只撤自己的补丁；这也解释了它为何在 O7 那一轮没暴露。
- 全量 `backend/tests` 仅剩 4 条既有失败；改动文件 `ruff`/`mypy` 全绿。
